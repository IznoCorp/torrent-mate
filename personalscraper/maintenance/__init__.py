"""Operator-upkeep package: re-scrape repairs + disk cleaning.

Hosts both maintenance modules — ``rescraper`` (targeted re-scrape repairs) and
``disk_cleaner`` (disk cleaning). Read/write maintenance operations over the
permanent library + indexer DB.
"""
