"""Executable helpers shipped with KanbanMate (e.g. the heartbeat hook).

Small standalone scripts invoked by orchestrated agents or the daemon. These
are leaf entrypoints and carry no import constraints relative to the other
layers (DESIGN §3.2).
"""
