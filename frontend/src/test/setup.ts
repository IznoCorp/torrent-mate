// Registers the jest-dom custom matchers (e.g. `toBeInTheDocument`) on Vitest's
// `expect`, and augments the `vitest` module's assertion types accordingly.
import '@testing-library/jest-dom/vitest'
