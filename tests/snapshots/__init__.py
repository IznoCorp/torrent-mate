"""Canonical Rich Console snapshot baseline (Pre-flight #7).

Hand-crafted deterministic event sequence used to record and verify the
RichConsoleObserver (Phase 2) and RichConsoleSubscriber (Phase 3) output.

The single immutable baseline file ``rich_console_canonical.txt`` is the
reference for Phase 2 sub-phase 2.4 visual smoke, Phase 3 sub-phase 3.5
subscriber rewrite, and Phase 3 sub-phase 3.9 phase-gate visual regression.
"""
