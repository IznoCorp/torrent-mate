{
  "name": "kanban",
  "version": "0.5.3",
  "description": "Kanban orchestrator CLI — the /kanban skill is a thin wrapper that shells out to the kanban CLI for all operations (install, init, seed, status, sessions, cancel, logs, doctor, run, poll).",
  "author": { "name": "LounisBou" }
}
