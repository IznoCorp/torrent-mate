# Phase 10 — Module size extraction (`existing_validator.py`)

Generated by the post-merge retrospective. Addresses item #20 of the 30-point audit. Module exceeds project hard ceiling.

## Gate

- Phase 7 complete (chain migration may have changed existing_validator.py LOC count; re-measure first).
- All registry tests pass.

## Goal

Bring `personalscraper/scraper/existing_validator.py` from 1069 LOC under the project's 1000 LOC hard ceiling (CLAUDE.md "Module size: soft warning at 800 non-blank LOC, hard ceiling 1000 LOC").

## Scope

- `personalscraper/scraper/existing_validator.py` — current 1069 LOC, target ≤ 800 (soft warning level).
- New extracted modules in `personalscraper/scraper/`.
- Tests in `tests/scraper/test_existing_validator*.py` — adjust imports.
- `scripts/check-module-size.py` — must continue to PASS.

## Sub-phases

### 10.1 — Audit existing_validator.py structure

Read the full file. List every public function/class with LOC. Group by concern:

```bash
wc -l personalscraper/scraper/existing_validator.py
grep -E "^(class|def) " personalscraper/scraper/existing_validator.py | wc -l
```

Identify natural fault lines (suggested splits per cycle 1 plan §2.3a "Phase 4 should consider extracting the repair helpers into a separate module"):

- **`existing_validator.py` core (≤ 500 LOC)** — `ExistingValidatorMixin` class skeleton, top-level coordination.
- **`existing_validator_repair.py` (new, ≤ 400 LOC)** — repair helpers extracted (NFO repair, artwork repair, episode merge).
- **`existing_validator_drift.py` (new, ≤ 300 LOC)** — drift-detection helpers (`verify_tvshow_scrape_drift`, `_infer_year_from_child_names`, `_local_show_seasons`).

Or whatever the actual structure suggests. Document the split decision in the sub-phase report.

Commit: `docs(scraper): audit + document existing_validator.py extraction plan`

### 10.2 — Extract repair helpers

Move repair functions (likely the ones building scrape-error recoveries) to `personalscraper/scraper/existing_validator_repair.py`. Update imports in `existing_validator.py` to re-export the helpers (keep backward-compatibility for callers).

Verify:

- `scripts/check-module-size.py` reports `existing_validator.py` <= 800 OR sub-1000.
- `pytest tests/scraper/test_existing_validator*.py` passes.

Commit: `refactor(scraper): extract repair helpers from existing_validator to existing_validator_repair`

### 10.3 — Extract drift-detection helpers (if needed)

If 10.2 alone is insufficient, also extract drift functions. Same pattern.

Commit: `refactor(scraper): extract drift helpers from existing_validator to existing_validator_drift`

### 10.4 — Verification + re-pin module-size baseline

After extraction:

- `python3 scripts/check-module-size.py` → exit 0.
- `wc -l personalscraper/scraper/existing_validator.py` → ≤ 800.
- Full test suite passes.
- `make check` exit 0.

Update CLAUDE.md or wherever module-size guarantees are documented if the extraction creates new files that should be tracked.

Commit: `chore(scraper): verify existing_validator extraction + check-module-size PASS`

## Phase gate

- `python3 scripts/check-module-size.py` exit 0.
- `wc -l personalscraper/scraper/existing_validator.py` ≤ 800.
- No public-API breakage (the re-exports preserve `from personalscraper.scraper.existing_validator import X`).
- All registry tests + scraper tests pass.

## ACC criteria touched

- ACC-12 (module-size guardrail) — must remain PASS.

## Cost estimate

- DeepSeek dispatch per sub-phase: ~5-10 min each.
- Total: ~30 min.

## Risk

Low. Extraction is mechanical (move functions + add re-export imports). Tests catch any missed reference.
