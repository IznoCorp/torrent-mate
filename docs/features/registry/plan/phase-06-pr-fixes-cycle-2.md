# Phase 6 — PR fixes cycle 2

Generated by `/implement:pr-review` after cycle 2 review of PR #27. 3 of 4 reviewers recommended MERGE; pr-test-analyzer blocked on missing regression tests (per memory `feedback_regression_test_per_bug.md`) + ACC-07 baseline drift.

## Gate

- Phase 5 milestone complete (`6177193`).
- PR #27 CI green on `feat/registry @ 6177193`.
- Cycle 1's 9 sub-phases verified closed by 4 reviewers.

## Goal

Land 3 missing regression tests for cycle 1 fixes + re-pin ACC-07 + 2 quality nits.

## Scope

- `tests/unit/api/metadata/registry/test_registry_fan_out.py` (or new file) — `_eligible` allowlist tests.
- `tests/commands/test_per_step_boundary.py` (NEW) — `per_step_boundary` close-on-exit test.
- `tests/commands/library/test_library_index_backfill_log.py` (NEW) — `library_backfill_provider_unavailable` log emission test.
- `docs/features/registry/ACCEPTANCE.md` — re-pin ACC-07 from `40` to `45`.
- `personalscraper/commands/library/scan.py` — hoist `_log` to module level (project convention).
- `personalscraper/api/metadata/registry/__init__.py` — `fan_out` AttemptOutcome schema cleanup (eligible providers should not synthesize `reason="other", detail="eligible"` — use a dedicated `succeeded` counter only).

## Sub-phases

### 6.1 — Regression test for `_eligible` strict allowlist

Add tests covering:

- Unknown class without `.circuit` AND unknown `provider_name` → excluded from `fan_out()`, WARNING `registry_provider_no_circuit` logged.
- `imdb` / `rotten_tomatoes` façade (no circuit) → still eligible.
- Class named `Fake*` without circuit → eligible (test-helper escape hatch).

Commit: `test(registry): regression tests for _eligible strict allowlist`

### 6.2 — Regression test for `per_step_boundary` close-on-exit

Spy on `registry.close`, run `with per_step_boundary(...)`, assert called exactly once. Also test exception path (close called even when body raises).

Commit: `test(registry): regression test for per_step_boundary close-on-exit`

### 6.3 — Regression test for `library_backfill_provider_unavailable` log

Stub `registry.get` to raise `UnknownProviderError`, call `library_backfill_ids`, `caplog.at_level(WARNING)` → assert one `library_backfill_provider_unavailable` record per missing provider with `provider=` field.

Commit: `test(registry): regression test for library-backfill provider-unavailable log`

### 6.4 — Re-pin ACC-07 baseline from 40 to 45

`docs/features/registry/ACCEPTANCE.md` ACC-07 expected stdout: `40` → `45` (current `pytest --collect-only` count after cycle 1 added 5 tests).

Commit: `docs(registry): re-pin ACC-07 baseline to 45 after cycle 1 added tests`

### 6.5 — Code review nits

- Hoist `_log` to module-level in `scan.py` (project convention — siblings use module-level `log = get_logger("cli")`).
- `fan_out` AttemptOutcome schema cleanup: eligible providers should NOT synthesize an `AttemptOutcome(reason="other", detail="eligible")` row. The `succeeded` counter on `RegistryFanOutCompleted` already conveys that. `attempted` is the documented "providers that were SKIPPED" list — keep semantics aligned.

Commit: `style(registry): hoist _log to module-level + fan_out attempted schema cleanup`

## Phase gate

- `make check` exit 0.
- ACC-07 PASS (count = 45 verified).
- New regression tests cover the 3 cycle-1 fixes that lacked them.
- Registry suite: ≥48 unit + 28 integration tests pass.
- Architecture: 67 PASS.
- Characterization: 6 PASS.

## ACC criteria touched

- ACC-07 — re-pinned in 6.4.
- All other ACC must remain PASS.

## On gate failure

Revert sub-phase commits since last gate, investigate root cause, re-run /implement:pr-review.
