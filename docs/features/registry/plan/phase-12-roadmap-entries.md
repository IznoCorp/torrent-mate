# Phase 12 — ROADMAP entries for non-goals + deferred-by-design items

Generated by the post-merge retrospective. Addresses item #19 of the 30-point audit. The DESIGN.md "Non-goals" + section §11 "Deferred-by-design" listed Web UI integration, active health scoring, and hot-swap as out-of-scope for the registry feature — but no follow-up roadmap items were created. This phase formalizes them.

## Gate

- PR #27 merged (registry framework live).
- `docs/ROADMAP.md` exists (project-level roadmap document).

## Goal

Convert the three explicitly-deferred items from DESIGN §11 + Non-goals into proper ROADMAP entries with priority + dependencies + rough scope, so future planning can pick them up.

## Scope

- `docs/ROADMAP.md` — append 3 new entries.
- `docs/features/registry/DESIGN.md` — update §11 to link to the new ROADMAP entries (closing the deferral loop).

## Sub-phases

### 12.1 — Web UI registry consumer

Add ROADMAP entry:

```markdown
## P2 — Web UI Registry Consumer

**Source**: registry feature DESIGN §11 deferral.

**Goal**: Expose ProviderRegistry status + operations to the future Web UI (P1 Web UI roadmap item). Surface live provider eligibility, circuit state, fallback history, fan_out attempted lists.

**Dependencies**:

- Web UI scaffolding (P1 Web UI Roadmap)
- `registry.status()` + `registry.operations()` (already shipped in P1 registry feature)

**Scope**:

- WebSocket subscription to `ProviderFallbackTriggered`, `ProviderExhaustedEvent`, `LockedCapabilityUnresolved`, `RegistryFanOutCompleted`, `RegistryBootValidated` events.
- REST endpoint `GET /api/registry/status` returning the dict from `registry.status()`.
- REST endpoint `GET /api/registry/operations` returning the dict from `registry.operations()`.
- UI panel: per-provider circuit state, per-capability priority chain, fan_out latency aggregates.

**Non-goals**:

- Hot-swap (separate ROADMAP entry — see P3 Hot-swap).
- Provider configuration editing via UI (config file is source-of-truth; UI is read-only).

**Estimated effort**: 1 sprint (5 days) after Web UI scaffolding lands.
```

Commit: `docs(roadmap): add P2 Web UI Registry Consumer entry`

### 12.2 — Active health scoring

Add ROADMAP entry:

```markdown
## P3 — Active Health Scoring (Registry)

**Source**: registry feature DESIGN §11 deferral.

**Goal**: Move from passive circuit breaker (per-call failure threshold) to active health scoring (periodic ping + rolling window + provider de-prioritization).

**Dependencies**:

- Registry framework (shipped P1).
- ProviderObserver protocol (already defined for circuit transitions).

**Scope**:

- New `ProviderHealthMonitor` running in background AppContext-scoped task.
- Each provider exposes `health_check() -> bool` (cheap synthetic call).
- Rolling window of last N health checks, exponentially-weighted moving average.
- `chain()` consults health score: providers below threshold are skipped (not removed — re-attempted on next health window).
- `registry.status()` includes per-provider health score.

**Non-goals**:

- Active load balancing.
- Per-region provider routing.

**Estimated effort**: 1 sprint (5 days). Risk: health_check budget for each provider must be defined to avoid quota burn.
```

Commit: `docs(roadmap): add P3 Active Health Scoring entry`

### 12.3 — Hot-swap provider configuration

Add ROADMAP entry:

```markdown
## P3 — Hot-Swap Provider Configuration

**Source**: registry feature DESIGN §11 deferral.

**Goal**: Reload `ProvidersConfig` on SIGHUP or config-file change without restarting the process. Currently registry is constructed once at AppContext init; config changes require restart.

**Dependencies**:

- Registry framework (shipped P1).
- `validate_config()` (shipped P1, used at boot — re-usable for hot reload).

**Scope**:

- File-watcher on `config/providers.json5` (using `watchdog` or polling).
- On change: call `validate_config()` → if PASS, atomically swap `ProviderRegistry._index` + `_priority_for_chain` + `_circuit_breakers`.
- Drain in-flight calls before swap (5s grace period).
- Emit new event `RegistryHotSwapped(...)` with diff summary.

**Non-goals**:

- Hot-swap of provider IMPLEMENTATIONS (only config). Adding a new provider class still requires restart.
- Distributed config (single-process only).

**Risk**: Race conditions on circuit breaker state during swap. Mitigate with explicit drain protocol.

**Estimated effort**: 2 sprints (10 days).
```

Commit: `docs(roadmap): add P3 Hot-Swap Provider Configuration entry`

### 12.4 — Update DESIGN §11 to link to ROADMAP entries

Edit `docs/features/registry/DESIGN.md` §11:

```markdown
## 11. Out-of-scope / Deferred

The following are deliberately deferred. See `docs/ROADMAP.md` for follow-up tracking:

- **Web UI integration** → [ROADMAP P2 Web UI Registry Consumer](../../ROADMAP.md#p2--web-ui-registry-consumer)
- **Active health scoring** → [ROADMAP P3 Active Health Scoring](../../ROADMAP.md#p3--active-health-scoring-registry)
- **Hot-swap configuration** → [ROADMAP P3 Hot-Swap Provider Configuration](../../ROADMAP.md#p3--hot-swap-provider-configuration)
- **Indexer migration** → [tracked in Phase 11 of this feature plan](plan/phase-11-indexer-migration.md)
```

Commit: `docs(registry): link DESIGN §11 deferrals to ROADMAP entries`

## Phase gate

- `rg "active health|hot.swap|Web UI" docs/ROADMAP.md` returns 3 distinct ROADMAP rows.
- `docs/features/registry/DESIGN.md` §11 contains 4 hyperlinks (one per deferral).
- No broken markdown links (`markdown-link-check` or similar tool exit 0 — manual verification acceptable).

## ACC criteria touched

- None directly (this is documentation work, not registry behavior).

## Cost estimate

- Pure documentation work, ~30 min Opus inline.

## Risk

None. Documentation-only.
