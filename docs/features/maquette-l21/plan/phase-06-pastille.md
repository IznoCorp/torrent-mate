# Phase 6 — DOIT-4's pastille, and R124's holds

**This is the ONE place this lot draws.** Every other part's rendering is validated (mission of
2026-08-19).

## What is measured today

The map's DOIT-4 row reads `partly`: R124 (`busy.py`) proves a legitimate act under a busy
pipeline is ACCEPTED; the VISIBLE half — the constitution's own « En file — pipeline en cours »
said of a MEDIUM — does not exist. `fr.json` holds one key, `screens.arrivals.queuedBold` =
« en file » (line 466), and it is the pipeline PASS's own strip on Arrivées
(`features/arrivals/page.tsx`, `data-part="live-activity"`, R66 reads it). **Nothing says it of a
medium.**

## The method, in this order and no other (§15)

1. **A named state FIRST**, in `engine/states.js`: a queue item whose ask arrived while the
   pipeline runs, or while §20's bound is met. A named state is harness, not surface.
2. **Then the rule**, seen RED: it reads the pastille on that state AND on the act under
   `busy.py`'s scenario — `setOperationOutcome` on `grabSeasonForFollow`, `requeueJourney`,
   `rescrapeJourney`.
3. **Then the drawing.**

**The operator judges it on his phone**: the design host on 8712 serves the built `dist`, so the
state named here is the state he sees.

## R124 — prove the repair, then add to it

**The brief orders a repair that has lost its subject, and the departure is recorded in the INDEX
(§ 7.1) and corrected in the BRIEF on this branch.** `busy.py` already raises both panels with
`raise_by_finger` — a hit test at the row's centre via `elementFromPoint`, the covering toast
emptied first, the tap's answer held so a missing path fails loudly. It landed inside L19
(`3d67325f9` → `9fa13da57`). L19's report § 6 row 16 records it; § 9 says it was left undone, and
§ 9 is the stale half.

So, in order:

1. **Prove the existing repair still bites, BY MUTATION, before adding anything.** A hold added to
   a rule nobody has re-proved is a hold on an instrument of unknown health.
2. **Take the residue the finger-driving did not cover**: the ACTION inside the raised panel is
   still `act.click()` (`:172`, `:222`), not hit-tested — so a button covered on a busy page is
   invisible to R124. Hit-test the action the way the row is hit-tested. *(Steward's ruling,
   2026-09-06: depth, not scope — same rule, same class of vacuity. It replaces the repair the
   brief ordered and adds nothing else.)*
3. **Then add this wave's holds**: the three new operations under the busy scenario, each queued,
   said, and never 409.

## D8 — the divergences this phase EXPECTS

The oracle WILL read divergences on the pastille's own state and on the states whose panels gained
a button. **Each is accepted WITH the register entry or clause it serves** (DOIT-4, B-301, B-302,
B-323) — never by moving the interface back — and **the reference is re-recorded ONCE at the
post-merge gesture, not by this wave.** A divergence on any OTHER state is a defect and a STOP.

## Gate

`run.sh --contracts`; the pastille's rule green after having been red; R124 green with its holds
counted and its mutation recorded; the oracle's divergence list matching the accepted set exactly.

## Commit

`feat(maquette-l21): an ask at the wrong moment says « En file », and R124 reads the finger it claims to`
