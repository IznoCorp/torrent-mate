# L13 — the rulings, one file (numbered, non-reopenable)

One line per ruling, its author, and the commit whose RESUME or phase amendment carries the full text (`git show
<sha>:docs/features/maquette-l13/RESUME.md`). L13a's 1–61 are copied here once; L13b and L13c APPEND, never rewrite.
An agent reads THIS file before its handshake — not the RESUME chain.

## L13a (2026-09-13)

1. `harness` is a declared bucket of `check-frontend-boundaries.py` — a·1, `123816c93`.
2. `design/src/harness/` is in the language guard's harness scope for the STRING arm only — a·1.
3. The product never depends on the instrument; `applyState` stays engine-side (a·3 part voided by 16) — a·1.
4. `drivenWithoutHistory(run)` is the engine's one addition; `driven` in the vocabulary — a·1.
5. `currentRender = null` was not carried — a·1.
6. `window.__navEchec = false` initialisation stays in the engine — a·1.
7. `installHarness()` takes no argument; the ≡ panel was `harness/panel.ts` (died at a·17-bis, ruling 31) — a·1.
8. The named states are eleven files under `harness/states/`, composed by `harness/index.ts` — a·1.
9. R1 — the four shell doors live in `lib/shell-doors.ts` (`toast`, `panel`, `bridge`, `screens`); `store` in `lib/store-access.ts`; app hosts are not import owners (fan-in) — a·2, `36cc38a4c`.
10. R2 — `features/acquisition/queries.ts` in `FAN_IN_EXEMPT`; the L13b phase deleting the engine's last read of `followActions`/`suggestions` removes it (b·5/b·13) — a·2.
11. The six product writes `window.__navEchec = true` stay — a·2.
12. `window.__mocks` stays published by the mock layer; only `app/` imports `mocks/` — a·2.
13. `knownMedium` fixture-backed until its killer phase (→ b·11 by 41) — a·3, `58d24cfc4`.
14. `navigationState` lives in `lib/navigation-entry.ts`; entry dials written ONCE (`ENTRY_DIALS`) — a·3.
15. `frame-domain-baseline.json` lib 18 → 23 — a·3.
16. `applyState` stays in the engine beside `render()`, handed in through `installPageRestore`; it leaves at b·7 — a·3.
17. `unwinding`/`currentRender` stay with `closeScreen` until a·5 — a·3.
18. `harness/publish.ts` publishes `__closeLayers` and `armedExit`; `BACK_WINDOW` converted — a·3.
19. `app/arrival.ts` holds `INITIAL_STATE` and `installArrival(store)` — a·4.
20. `frame-domain-baseline.json` app 130 → 138 — a·4.
21. `fixture-register.json` `$anonymous.count` 1 → 0; `boot_order.py` and `bridge.py` (f′) re-aimed — a·4.
22. Q4: a dead `#screen` rung is REMOVED, a field is re-aimed; `bridge.py` reads `[data-key^="mediaSheet:"]` — a·5, `84fee5532`.
23. `window.__close`'s readers use `window.__panel.close()` / `__bridge.back()` — a·5.
24. `openDetailSheet` went with the generic `sheet` branch; `boot_order.py` intact — a·5.
25. The join is `sheetFor`'s four tiers in `build-mock-seeds.py`; `ids`/`poster` on list schemas; `Follow.ids` non-null — a·6.
26. B-497 filed: `build-mock-seeds.py --check` reports converted seeds as orphans (`--write` would delete 22) — a·6.
27. OPERATOR: the cut is B — L13a → L13b → L13c, three pull requests, one reader round and Mac walk each.
28. OPERATOR: D-L13-1 = A, ratified as written; L13b's STOP E lifted.
29. OPERATOR: Q2 = B, the ≡ harness panel dies (refined by 31).
30. a·7's four bare `sec` repairs accepted as a conversion (45 divergences named) — a·7.
31. The ≡ PANEL dies alone as a·17-bis: `harness/bar` and `#notesBtn` STAY; `hiding.py`/`chrome.py` do not move; `panel_verbs.mjs` is not a reader — steward, `be6f2a32c`.
32. R80's floor moves to the MEASURED count, every pair added/removed named — steward, a·8.
33. `.poster` is not paired (`-webkit-touch-callout`) — a·8.
34. Action buttons are axes of `actionButton()` (`kind` × `tone`) — a·8.
35. `.btnprimary:disabled` moved to `base.css` unlayered; c·3 draws the disabled state — a·8.
36. Every log under `/private/tmp/tm-l13a/` (L13b: `/private/tmp/tm-l13b/`), gate logs kept until the merge — a·8.
37. The card is PARTS in `ui/card.tsx`, factories in `ui/variants/card.ts` — a·9, `0cb5ce960`.
38. A card's domain composition stays in its feature; features never import each other — a·9.
39. No card rule dies before `cardHTML` does — a·9.
40. The card's French lives in `surfaces.card.*`; poster-box floor and `LOCAL_DETAILS` re-taken per phase — a·9.
41. `LIBRARY`, `INCOMPLETE`, `knownMedium` die at b·11 (exact membership read), not a·10 — steward, `c8995c858`.
42. String-composed surfaces get a MARKUP spelling in `ui/` (`tileMarkup`, `swipeRowMarkup`, …) — a·10.
43. R77 reads identities, never a class-string equality; `page_host.py` at its 999 ceiling — a·10.
44. Tile badges' no-fill defect carried, not repaired (register at the gesture) — a·10.
45. `POSTERS` died at a·13 with `poster` on DecisionCandidate/DecisionChoice — steward, `a6c91fc57`.
46. `page_host.py` 999/1000: compaction refused, zero-net-line edit allowed and said, the first added line extracts a module — auditor.
47. A served field differing from the engine's answer is a JOIN miss repaired in the data (base-title fallback) — steward, `1e34b9660`.
48. `heavy.sh` counts « Pages speculative » — auditor order 3, `1a73ff2ea`.
49. `mutate.sh` reads a rule's exit code as a fall — steward, `4c0e1d036`.
50. a·14 lands as two commits — steward.
51. a·14.2 stays in L13a: the crossing carries what the tap knew into the navigation ENTRY (amended by 55) — auditor, `379279643`.
52. `SEASONS` → a·14.2 with the `window.__mocks` accessor (amended by 53) — steward.
53. `SEASONS` stays until b·11: the engine's fixture LIES (Silo S4); the follow panel must agree with the SERVED sheet — steward, `4ced4b26d`.
54. The ONE `window.__mocks` seed accessor lands sheets-only, harness-read — steward.
55. Reading (A): a tap primes title and poster only; year/genre/synopsis/cast skeletons in flight; D8-named on every tap (reader line + Mac step owed) — auditor.
56. `screens.media.synopsisUnread` reads « Synopsis non lu. » — steward.
57. a·16 lands settings only; the React-side engine support dies at b·13; twelve interface constants need homes — steward, `c194fca54`.
58. The lot's folder `docs/features/maquette-l13/` survives L13a, dies at L13c's gesture — auditor.
59. `residue.py`'s reader half → `harness/factories.py`; pull spinner utilities on the child `.spin`; #ptr utilities erased by `__reposPTR` filed for b·8 — steward, `e2f510a8b`.
60. Narrow authorization: reference docs edited ONLY to re-cite a deleted full path `@60530dbd8`; CLAUDE.md never on a peer's word — steward.
61. The STOP B (cold follow panel loses « Voir la fiche ») repaired IN L13a as a restoration: the panel's facts follow the identity read, re-produced in place, no history entry; mutation seen; empty-key query entries filed for b·11 — auditor, `57f3e81af`.

62. OPERATOR (19:2x, relayed): the implementer's context gate is 80 %, not 60; pre-dispatch = gauge + last phase's measured cost ≤ 80 — `37e54d0fd`.
63. Auditor order 15: the wave does not write IMPLEMENTATION.md's « In flight » row; the register's `fixed #<PR>` rows stay in the closure commit — `37e54d0fd`.

## L13b — append below, numbered from 64

64. L13b's resume is a NEW file `docs/features/maquette-l13/RESUME-L13b.md` (state block ≤ 40 lines + append-only ledger); `RESUME.md` is L13a's and dies at its gesture (ruling 58) — steward, handshake answer 2026-09-13.
65. b·2: the account's key is `data-account`, valueless (the registry dispatches on the dataset KEY); the four sites (`index.html:285`, `address.py:119`, `bugs.py:106`, `focus.py:37`) change by scripted exact-string replacement, because `rename-identifiers.py` cannot move one key on one element while `data-sheet="plus"` keeps it; `entry.py:99` → `window.__entry.signOut()`; fan-in `app/entry.ts` 3 → 4 = the ceiling. A log a RESUME, an amendment or a commit body CITES is kept until the merge — steward.
66. Auditor order 24: `run.sh --contracts --oracle [rule.py …]` is the phase gate — one build, contracts + named rules in one pass, guards, oracle, one verdict block; every later b-phase gate is that invocation, its wall clock reported — `108904b51`.
67. Auditor order 25: the contracts tier's fan-out is measured at `TM_HARNESS_JOBS=3` with `vm_stat` and `vm.swapusage` before/after; swap moved → back to 2 — steward.
68. Auditor order 26: no local `make check` before L13b's pull request — CI's `test` job is the authority; the pre-PR gate is `make lint` + the full suite + `--a11y` + `--compare` + the pre-push pytest — steward.
69. Auditor order 27: `run.sh` and `mutate.sh` bound every rule invocation (10 min, `TM_RULE_TIMEOUT_SECONDS`) and name a rule past it « TIMED OUT », an INSTRUMENT fall never counted as « no rule fell », the file restored and the served copy released; `heavy.sh` writes its pid beside its name and breaks a lock older than 45 min ONLY when that pid is gone — a holder alive and silent is held off and said aloud. `panel.py`'s own hang stays filed, not repaired — steward.
70. b·3: the `mediasheet` witnesses are `transition.py` (R115) and `paths_to_sheets.py`, both falling by name with the registration removed; `screen_addresses.py` reads the sheet by ADDRESS and cannot witness a tap (phase-b03 amended); `ep` → `pop.py`. B-290's `__TSR_index` reading is taken by a throwaway probe under the mutex before and after the move, never committed; « B-290's index reading is owed to phase b·9's R-L13-a/b/c when they bind to numbers » — steward.
71. Trial (« essai », auditor order 31): baseline figures are no longer copied into commit bodies — the JSON diff is the record; the body names the file and the direction in one line, ≤ 12 lines — steward, from b·3.
72. Trial (« essai », auditor order 32): a CONVERSION pull request cites no constitution §§ (nothing observable changes); a behaviour pull request — L13b's — still does — steward.
70-bis. b·3's second `mediasheet` witness is withdrawn: `paths_to_sheets.py` (R122) reads reachability in the markup (lines 71/73) and cannot witness a tap; `transition.py` falling by name on four holds is the witness — steward, correcting 70.
73. b·4: DESIGN § 6 is wrong on `data-resolve` (it means « pick this candidate », B-474) — one arrivals-owned opener `data-resolution="<folder>"`: the now-tab blocked foot with its title, the arrivals stuck foot EMPTY (the first stuck folder, behaviour kept; carrying the title is a repair filed beside B-474, two_picks.py:165 its witness), the takeable foot `data-take`; `.cfoot` and `act=resolve` die, the follow panel's `data-resolve` untouched. `actionLeave` dies in b·4, `actionResolve`/`actionTake` in b·5 with their last callers. `next` draws no « none left » message; its hold is decision.py's — steward.
74. b·4 moves everything except `pipe`: a feature writing the `pipe` store key is refused by `check-state-ownership.py` (component ceiling 0, not raised); its conversion to `/api/pipeline/status` + runPipeline/killPipeline is phase b·12, after b·11 and before b·13 — steward.
75. b·5: `data-sheet="plus"` → valueless `data-more` (`data-sheet` dies with its branch); `data-act="add:N"` → `data-add="N"`, add_footer.py / audit2.py / bugs.py re-aimed with counts unchanged; mechanism as ruling 65; the sentence « `data-sheet` keeps only acquisition's `plus` » in bugs.py, focus.py and address.py amended in the same commit — steward.
76. Auditor order 34 (2026-09-14): the 05:00 reboot wiped `/private/tmp`, L13b's earlier gate logs included; gate logs live under `~/Library/Logs/tm-l13b/`, the mutex and the tests lock stay under `/private/tmp` — steward.
77. `mutate.sh` refuses a rule path that does not exist before mutating (« RULE NOT FOUND: <path> », exit 64) and reads exit 2 with no FAIL line as « RULE CRASHED », an instrument fall, never « FELL »; red first in `tests/scripts/test_mutate.py` — steward, after b·5's void series #1, `c5eeff002`.
77-bis. `mutate.sh` starts the 8899 host exactly as `run.sh` does when nothing listens and stops the one it started (a wrapped run stops the host IT started — « left running » holds for unwrapped runs only); a `Traceback` with no FAIL line is « RULE CRASHED » whatever the exit code; same commit, red first — steward, after b·5's void series #2, `c5eeff002`.
78. The forwarder floor follows the tree down: `test_it_actually_found_forwarders_to_check` reads ≥ 4 with one dated docstring line; re-aiming `check-markup-contracts.py`'s arm at the feature's `registerVerb` writes would widen its subject (measure 1) for a count b·7 takes to 0; when b·7 empties it the floor becomes « the count it found » or the test dies with its subject, said then — steward.
79. b·6: (a) the library registers `selected-title` only (value = the title, emitted in selection mode, grid and list) — the registry answers the first REGISTERED dataset key in attribute order and stops the tap, and a tile emits `data-tile` before `data-mediasheet`, so a registered `tile` would swallow every sheet tap; `data-tile` stays the index the rules read; hold first in `selection.py`. (b) the follows swipe emits `data-pause`/`data-remove=<title>`; those verbs and the library's `del` call `window.collapseCard?.()` until b·8 moves the gesture state; « chercher » emits `data-toast="<label> — <title>"`. (c) the « est suivi » warning reads a `followedTitles` door in `lib/shell-doors.ts`, filled by acquisition's `installFollowActions`. (d) `clearq` → valueless `data-clear-search` (library) / `data-clear-filter` (acquisition), `page_host.py` re-aimed, count unchanged — steward.
79-bis. Correction of 79(b), « behaviour identical » WITHDRAWN — the engine's branch order decides: `del` (legacy.js:2462) answers before `.act` (:2474), so library rows never collapsed — the library's `del` = panel.close() + openDeleteDialog, no collapse; `toast` (:2327) also answers before `.act`, so « chercher » becomes an acquisition verb `search-again` (`data-search-again=<title>`) that settles its `.swipe` row as `.act` did (collapseCard when it is the open card, else a transform reset) then toasts « <label> — <title> »; pause/remove settle their row only from a swipe button (a `.swipe` ancestor), the panel path unchanged; the reaches through window (`openCard`, `collapseCard`) are owed to b·8 — steward.
80. Order 35 (the steward set `core.hooksPath` to the RELATIVE `hooks`; the absolute value made every worktree run main's pre-push): `hooks/install.sh` already writes the relative form (line 53) and rewrites any other value, an absolute path included (line 50); its header lines 6–7 are amended — every worktree shares `.git/config`; no test reads install.sh — steward.
81. `run.sh` refuses any positional argument it does not read (« run.sh: rule names are only read with --contracts --oracle », exit 64, before any build); red first in `tests/scripts/test_run_sh_builds_once.py` — steward, after b·6's two vacuous « green » steps (`run.sh --contracts selection.py` read no name).
82. b·6's two names no rule tapped get their holds FIRST: `search-again` → `pause_verb.py` (R132) « a pending follow's « Chercher » says the search and the row comes back to rest », red with the engine's `.act` fallback toast removed; `clear-filter` → `follows.py` gains its first named check (a FAIL line printed at once, exit 1, its docstring saying it now holds), red with the engine's `else store.write({ filter: "" })` disabled — steward.
83. Rule numbering: L20 runs beside L13b on `feat/maquette-l20` and binds R178–R187; any NEW rule file L13b creates (b·9's R-L13-a/b/c, b·11's « the follow panel agrees with the sheet ») takes R188 and up — steward.
84. R164 (`selection_survives_the_tab.py`) fell twice under the 28-named gate load and passed alone and once under load: its detail string is armed to name what hid the bar (reporting only, the check unchanged); ONE clean gate on the committed head, read as it comes (office rule 11) — red with the mechanism → the fix in b·6; green → filed in the ledger and a register row, owner b·13's full suite; then the docs commit, the push and the MIDPOINT full suite; b·7 does not open in this session — steward.
85. No register row for the midpoint fall: a defect introduced and repaired inside the same unshipped branch belongs to the ledger and the pull request's body, not to the register (which holds what shipped or stays open). The MECHANISM gets a durable line instead — one dated amendment in `plan/phase-b05-acquisition-verbs.md` and one trap in `frontend/maquette/README.md` — because every later phase moves verbs into the same capture-phase registry — steward, after R55's repair `4210f72d5`.
86. b·7's five opening questions, in order: (a) Q1 = A on the measurement — `check-state-ownership.py` reads `app/` as a component bucket (`COMPONENT_BUCKETS`, line 55) at a component share ceiling of 0, so the FRAME registering `pipe` is refused exactly as a feature would be (ruling 74 holds wherever the verb lives); the engine's click listener therefore SURVIVES b·7 with exactly one named branch, `pipe`, and its death moves to b·12 beside `pipe`'s conversion. (b) the four emitters where `data-panel` preceded `data-mediasheet` on ONE element are reordered by scripted exact replacement, `data-tile` untouched; no declared priority in the registry (new apparatus); `transition.py` (R115) is the hold that would have caught the inversion — it taps a tile and reads the address. (c) the forwarder floor test dies with its subject. (d) `applyState` → `app/layers.ts`. (e) the `render()` re-aim goes ahead on the MEASURED figures — 43 call lines in 9 files, not the plan's « 23 in 10 »; `page_host.py` same-line, no docstring line added to the nine files (the hold's reading is unchanged; the body and the RESUME say it) — steward.
87. The forwarded-value ARM dies with its subject too, not only its floor test: with the seven verbs moved the engine forwards NOTHING and the arm refuses an empty corpus by design (« no `store.write({f: …dataset.x})` handler found », exit 1), so an arm kept green over nothing was never on the table. Pattern, arm, helper and tests deleted; the loss is RECORDED — the guard's own header, one dated line in phase-b07, a paragraph in the pull request body, and register row B-513 (the « markup literal nobody compares » class is unguarded; candidate re-aim at the registry's dataset reads, after L13, the operator's or the auditor's call) — steward.
86-bis. Accepted as MEASURED, the ownership arm's exit codes deciding: the engine's listener keeps TWO named branches, `pipe` and `phase`, until their conversions (`pipe` at b·12; `phase`'s owner named there too), and `applyState` stays in the engine to b·13 with the reason written on `app/layers.ts` — ruling 16's move is re-dated to the phase that deletes the engine. `render()` goes to b·13 with its seven callers; the `landOnPage`/`resetLandingDial` door is ruling 9's shape — steward.
88. STANDING CHECK for b·8 to b·13, from b·7's three defects: before a name moves, the phase file's dated line answers « WHO ELSE answers this click on this node, and in which phase — capture or bubble, document or element? ». The press's swallower, `lib/stacked-surface.ts`'s replay, the engine's remaining branches and `preventDefault` on links are the four known answerers. The reference-reading trap — a roll-back of `design/src` followed by `git checkout HEAD --` discards uncommitted work, so commit first — joins R55's sentence in `frontend/maquette/README.md` — steward.
89. b·8's five: (a) the drag guard moves to `lib/` with `stopImmediatePropagation`, registered before the registry; the one-tap hold proves it and a threshold mutation fells it; no register row (unshipped, ruling 85), ledger and pull request body, and the live hole it closes is ruling 88's first catch. (b) The two gesture getters a rule reads (`openCard`, `swallowClick`) are published from their new owners; the five nobody reads die with the `defineProperties` block. (c) The feature imports the row's verbs from `lib/`, the `window.openCard`/`collapseCard` reaches go (ruling 79-bis's owed); `drive.ts` imports the indicator's reset. (d) B-337's hold stays in `pause_verb.py` (R132) and the register row stays OPEN: the hold exists, the phone reading is the operator's. (e) `guard`, `travel` and `indicator` enter the vocabulary; the rest is built from words already there — steward.
90. b·9's four: (a) the close-then-wait inventory is NINE, not the design's ten — the two `setTimeout(() => row.remove(), COLLAPSE)` sites are a row removal waiting for its own collapse, not a layer close waiting to be gone, and they stay; the count and the reason go on the phase's dated line. (b) R188 is `ladder_entries.py` (the pops are counted) and R189 `redraw_entry.py` (a redraw does not stack); R190 stays unbound, and R-L13-c is `exits.py`'s reversal under R103's own number. (c) The STRICT oracle reading: D-L13-1 changes history and what Back does, nothing at rest, so ZERO divergence is expected and ANY divergence is STOP B — never accepted under the decision's name. (d) B-290 and B-397 close with `fixed #<PR>` in b·13's closure commit (ruling 63); b·9 records their readings — steward.
91. b·9 Q1 = A: `take` is an ACT, not an arrival (the engine unwound the panel's entry at 37e54d0fd) — R188 holds the four arrivals with three readings each and keeps `take` as the act's counter-hold (its entry unwinds, one Back reopens nothing), re-aim said in the docstring, the body and the phase's dated line; S1–S4 accepted: the sheet entry `{ layer, kind, subject, openedOn }` composed once in `lib/navigation-entry.ts`, B-397's general path decided on the entry read, the nine timers replaced by `close(true)` inside `go()`'s commit and REPLACE navigations for the two route-to-route leaves, `exits.py`'s source hold written first — steward.
92. `run.sh` refuses a named rule whose file does not exist, resolving each argument WHOLE (its directory must be the harness's), exit 2 before any build; red first in `tests/scripts/test_run_sh_builds_once.py` (a space-joined word read « 19 rules (1 named) », exit 0) — steward, after b·9's first gate ran one of thirty-five rules and read green, `b64ae8f76`.
93. Order 38 (operator via the auditor): « N-bis » is reserved for a CORRECTION of phase N; an inserted phase takes a whole number and shifts the ones after it. L13b: b·10 stays; the membership read (formerly « 10-bis ») → b·11, the pipe + phase (formerly « 10-ter ») → b·12, « legacy.js dies » → b·13; phase files, INDEX, RESUME, BRIEF and every citation on the branch renamed (rulings 41, 53, 74, 86-bis's text amended, numbers kept); history citations `@sha` untouched — steward.
94. b·11: Q1 = A — the two backend demands (exact title(+year) membership → { inLibrary, rows, incomplete, ids }; ruling 55's year+kind on QueueCard/LibraryItem/LibraryRow/IncompleteShow) are a dated line in phase-b11 and the PR body, carried into `docs/reference/backend-demands-architecture.md` by the steward's docs PR; Q2 = yes — the maquette contract gains ONE operation `readLibraryMembership` (types regenerated, conformance test, `mocks/handlers/membership.ts` from the whole seed); R191–R193 bound; R-b11-1's red walk without reload; the follow kind's `needs` a function of the subject; the oracle divergences measured and named before the gate, the six disagreeing titles the whole allowed set — steward.
95. b·11 seasons drawing = A: the panel's season summary adopts the sheet's three conventions (aired 0 → « à venir »; aired unknown → « owned/? »; not owned → « N ép. », no shortfall) through the same i18n keys — the operator's ruling of 2026-09-11 (B-380) applied; R192 holds TEXT equality panel vs sheet per season row, Silo named; Silo's figure corrected (served sheet four seasons, engine table three) — steward.
96. The frame-domain ratchet raise is accepted: lib/ 23 → 28 (`lib/membership.ts` and `lib/season-rows.ts`, the API paths and contract schema name of reads two features share) and app/ 138 → 139 (`app/addressed-panels.ts` reading the contract's `inLibrary` for `heldMedium`), reasons in `scripts/frame-domain-baseline.json` — steward.
97. b·12: Q1 = A — `Pipeline` gains `state: PipelineState` taken VERBATIM from L20's phase 4 (`origin/feat/maquette-l20`), the mock projects it from `pipelineState`, the verb calls run/kill then refetches the status, named states and rules drive the layer through `window.__mocks.setPipelineState` + refetch (landed as `window.__pipeline(state)`, `applyState({ pipe })` routed there); B refused (a reload forgetting a running pipeline). Q2 = A — the retry with no onRetry becomes a frame verb re-asking every active read (`data-retry`); `phase` stays a harness-only knob written by named states through applyState; the engine branch dies. b·13 is the next agent's unless b·12 measures under 8 — steward.
98. STOP D: b·13 measured, not opened (2026-09-15, on 289cfd2e1). legacy.js holds 1 600 non-blank lines of LIVE subject — interface constants (ST_LABEL/ST_TONE, URGENCY, GROUPS, EP_LABEL/ORDER/SWATCH, REASON_*, DECISION_STATE*, VIA_LABEL, MAINT_TOPICS, CADENCE_CRON, MOIS, TODAY, AUDIOS), 13 unconverted fixture families (JOURNAL, SERVICES, SERVICES_PANNE, DISKS, INDEX, DEPENDENCIES, ERRORS, EXECUTIONS, SECRETS, ACCOUNT, POSTERS_HD, RESOLUTIONS, LIB_PAGE), helpers (svgIcon, escapeHtml, baseTitle, initialsOf, cadenceFR, nextSearchFR, stLabel, stFraction, gridBadge), the settings state machine, render()/currentState/applyState/beforeReset, the search mount, toast/openSheet/closeSheet/showSignIn, actionDelete; `window.__referentiel` read by 43–45 product files through nine slices; `engine/engine-shape.ts` 41 call sites in 14 files over 23 families; 20 files import engine/. The measured proposal is a re-cut (a) constants and helpers find homes, (b) the fixtures convert or move to seeds, (c) the settings machine, render/applyState, the search mount and toast/openSheet leave the engine, (d) __referentiel and the slices die, (e) engine-shape's families rename to the contract's names, (f) the file and its instruments die — routed to the auditor (re-cut inside L13b, or L13b closes at b·12 and the residue becomes a conversion sub-lot). Meanwhile the full suite, --a11y and --compare run on b·12's head — steward [31ca3c].

99. Q5 = B (auditor, § 10 delegation, 2026-09-15 03:4x, after two hours without the operator's word and zero implementer live; reversible by his word while L13b's pull request is unmerged — A would then reopen the branch): L13b CLOSES at b·12. Its behaviour subject is landed — the verbs by feature, the gesture listeners, the timers, B-290 / B-397 / B-275, the engine's click listener dead, `legacy.js` 2 755 → 1 601. `phase-b13-legacy-js-dies.md` is VOID for L13b: its subject is a CONVERSION (ruling 98's figures) and becomes a new sub-lot, **L13r — The engine's residue**, run between L13b and L13c, phases r·1–r·6 = the six residue files renamed `phase-r01-…` … `phase-r06-…` (INDEX.md's « Residue (pending Q5) » heading → « L13r — The engine's residue », the lot's order L13a → L13b → L13r → L13c; the design's three-sub-lot table gains its row in the lot's docs PR, with the measurement). **L13b's closure, and nothing else, is `Agent : l13b 5`'s scope**, in this order: (1) the version bump (patch, one commit — read `personalscraper/__init__.py`; main is 0.98.93) and the residue renames + INDEX + this file's numbering + the RESUME state block, pushed (the full pre-push suite runs once, under the tests lock); (2) the pull request READY — title `feat(maquette-l13b): the engine's verbs and the ladder's shape — …`, a BEHAVIOUR pull request, so it cites the constitution §§ it serves (order 32 exempts conversions only); body in the L13a shape, figures written ONCE on the final head, bodies of commits read for order 31's fate (≤ 12 lines, no baseline figure); (3) the closure commit on the branch: `BUGS.md` rows B-290, B-397, B-275 → `fixed #<PR>` (ruling 63 / 90-d, moved from b·13 to here), B-512 and B-513 stay as filed, `IMPLEMENTATION.md` « In flight » stays « None » (measure 4's consequence), pushed on the docs-only path (seconds); (4) the report, then stand down. The reader round follows (order 17: its worktree pinned at 18be0f758 now, re-pointed at the final head), the merge on green, the steward's gesture, the lot's docs PR (the L13b rows, the auditor's method lines, the table's fourth row), and L20's phase 8 unblocks — steward.

## L13r — append below, numbered from 100

100. r·2 (STOP D at its opening, on 0d313dd2a): Q1 = A — the harness readers of the dead families (`machine.py` BLOCKS, `address.py` `ACCOUNT.mail`, `content.py` `CADENCE_CRON`, `season_family.py` and `season_grab_unfollowed.py` `TODAY`) are re-aimed at the cache on `machine.py`'s `SCHEDULERS_SOURCE` precedent, each said in its docstring and the body with ONE mutation by name; R85 (`mocks.py:116`) re-aimed to « the product clock equals the mock clock », its old subject dead with the copy. Q2 = A — `lib/clock.ts` `today()`, filled by `app/` at boot with `scenario().now` under `__MOCKS_BUILT_IN__`, else the real date, published for the rules by `harness/publish.ts`; no contract field. Q3 = A — `posterHighDefinition` on the `Suggestion` schema (ruling 25's precedent), one demand line « the suggestion list serves the HD poster the sheet already serves ». Q4 = C — `features/account` installs the topbar avatar through a `lib/` door; `app/` stays at 139, `lib/` may take one door (ceiling line before and after). Cost ≈ 13 > mean 11, reason on the phase's dated line — steward [31ca3c].

101. r·3 (STOP D at its opening, on c29a5c5c7; measured ~25 points): Q1 — the cut is NUMBERED (order 38): r·3 « the engine's product verbs » (the settings machine → `features/settings/`; `addVerb` → `features/acquisition/` via `fr.json`; `actionDelete` → `features/library/` via `fr.json`; the press → `lib/press-arbitration.ts`; `mountSearch` dies, `follows-filters.tsx` owns `#follq`; the reader-less names die), r·4 « the engine's frame verbs » (`render` → `app/` with one `lib/` door for its 18 product readers; `applyState` → `app/layers.ts` per 86-bis; `toast`/`closeSheet`/`showSignIn`/`__navEchec` → the existing doors); the former r·4/r·5/r·6 become r·5/r·6/r·7 (files `git mv`'d, INDEX, RESUME, BRIEF references, one docs commit before r·3's code); the MIDPOINT suite runs after r·4. Q2 — the harness's bare names (`state`, `store`, `view`, `applyState`, `render`, `closeSheet`, `showSignIn`, `toast`) are republished from `harness/publish.ts` under the same names (a·2's precedent), the harness imports switched to the owning modules, 0 re-aim. Q3 — `SETTINGS_STATE` / `settingId` republished on `__referentiel` from their module as in r·1, owed to r·5 on the ledger. Q4 — a rule first ONLY where none taps the moved name, grepped at each phase's opening, red on the engine before the move, then mutated by name; the existing rule covering each other name is said. Q5 — `openSheet` dies; a docstring-only citation in `panel.py` is edited in the same commit if its source check reads the name. This session takes r·3, r·4 and the midpoint suite, then stands down; `Agent : l13r 2` takes r·5 on — steward [31ca3c].

102. r·4 (STOP D at its opening, on a8b425490): Q1 = A — `applyState` and `installPageRestore(applyState)` stay in the engine until r·7: 86-bis's reason is unchanged (`check-state-ownership.py` refuses a store write whose argument it cannot read outside the engine; extending the guard is new apparatus, measure 1; rewriting the restore is behaviour, not this phase). The rest of r·4 moves: `render` → `app/redraw.ts` behind `lib/shell-doors`' `redraw`; `toast`/`closeSheet`/`showSignIn` republished from `harness/publish.ts`; `__navEchec` → `app/page-switch.ts`; `toastUndo` dies. OWED TO r·7: `applyState`'s honest home when the file dies is a restore written with LITERAL keys in `app/layers.ts` (`entryPatch`'s keys spelled out), held by R188/R189/R190 and the named states — r·7's opening STOP D carries it with the key list measured. Q2 = yes — `state`/`store` and `view` republished from `harness/publish.ts`; `select` and `icons` stay the engine's, owed to r·7 — steward [31ca3c].

103. r·4 (mid-phase STOP): Q1 = A — the store key `notFound` is an INTERFACE key (the unknown address the not-found page names; `lib/addresses.ts` composes it), misfiled beside the served queue list of the same name; reclassified in `check-state-ownership.py` with the reason in its comment and the commit body; no test in `tests/scripts/` holds the classification table. Q2 = C, measured — the guard lets a feature write interface keys, so `sugCount` and `sugLoading` (the deck's paging state, written by its own feature) are reclassified the same way and the stale `ENGINE_OWNED` exemption of `discover-feed.ts` is deleted; the ceiling follows the measured count, 7 → 0 — steward [31ca3c].

104. r·5 (STOP D at its opening, on 9b81c5f49): Q1 = A — an `icons` door in `lib/shell-doors.ts` filled by `app/shell.tsx` at boot before anything is drawn (ruling 9's precedent), the product's icon reads through `useEngineDrawing()` or the door, the nine `useXReference()` hooks die by `scripts/rename-identifiers.py`, lib/ stays 28. Q2 = A — the slices become `features/<f>/types.ts` (git mv, importers scripted; account's, left empty, is deleted). Q3 = A — `settings.py`/`page_host.py` read `window.SETTINGS_STATE`/`window.settingId` (engine-published, dying at r·7, owed on the ledger); `__baseTitle`/`__dateLabel` published from `harness/publish.ts` from their r·1 homes; `followed_sheet_act.py`, `season_family.py`, `pop.py` re-aimed, counts unchanged, said in docstring and body, one mutation by name per re-aimed file. Q4 = yes — the reference-slice arm dies with its subject; B-513's class stays unguarded, said in the body. Cost ≈ 14 > 11, reason on the phase's dated line — steward [31ca3c].

105. r·6 (STOP D at its opening, on 533c160e5): Q1 = A — r·6 « contract names » is cut, numbered (order 38): r·6 queue cards · r·7 arrivals decisions + pipeline · r·8 follows + incompletes · r·9 suggestions + search + releases · r·10 library + settings + maintenance + account + SCHEDULERS · r·11 the sheet (SHEETS_RAW), then `engine-shape.ts`, its test, `check-mock-seeds.py --arm lossless` and `fixture-projections.json`'s families die · r·12 `legacy.js` dies + the full gate + the pull request; one docs commit (files, INDEX, RESUME, BRIEF) before r·6's code; each ≤ 15 with its estimate on the dated line. Q2 = A — per family the query drops the projection and its type becomes the contract's schema; `tsc`'s diagnostics are the exhaustive list of typed readers, each site corrected by a scripted replacement at the reported line:column (ruling 65), the diff re-read; untyped readers (`QueueCard`, `MediaSheet`'s `Record`, the markup `.ts` templates, the cache-reading rules) receive the contract type FIRST in the same sub-phase; the rest grepped by family name; oracle ZERO. B (a type-scoped mode of the rename tool) refused — measure 1. Q3 = yes — re-aims said in docstring and body, one mutation by name per re-aimed rule; above five in a sub-phase, one mutation of the query per family that fells all its named rules, each FAIL line read — steward [31ca3c].
