# Pipeline Run — 2026-05-25 23h49

## Status: STOPPÉ AVANT DISPATCH (read-only)

## Matrix
- Version: 2.1
- Stale-detector findings: 30 stale event entries (WARNING, not blocking). Top clusters: Pré-run recovery (6), PROCESS:cleanup (4), TRAILERS (4), library-scan (4), TMDB validation (6), PROCESS:clean (2), ENFORCE (2). Also 369 coverage gaps in code (error/recovery paths not yet catalogued). See PHASE 3 final analysis.

## Pré-analyse
- Torrents complets dans qBit: 36
- Torrents trackés dans `ingested_torrents.json`: 36 → **0 nouveaux à ingérer**
- Médias en staging: 5 films / 10 séries (15 total)
- Items dans 097-TEMP: **0** → SORT déclenchera vraisemblablement `sort_fast_skip`
- Pré-recovery: aucun `_tmp_dispatch_*`, aucun `_tmp_ingest_*`, pas de lockout qBit
- Espace disque:
  - SSD (staging): 370 GB libre / 932 GB total (61% used)
  - Disk1: 1.7 TB libre / 13 TB (87% used)
  - Disk2: **290 GB libre / 3.7 TB (93% used)** — TIGHT
  - Disk3: 927 GB libre / 3.7 TB (76% used)
  - Disk4: 680 GB libre / 2.8 TB (76% used)
- Résultat attendu (fast-skips probables):
  - INGEST: 0 ingestions nouvelles (tracker exhaustif)
  - SORT: `sort_fast_skip` (097-TEMP vide)
  - CLEAN/SCRAPE/CLEANUP: fast-skips possibles si NFO valides + aucune pollution
  - ENFORCE: fast-skip si structure conforme
  - VERIFY: validation
  - TRAILERS: **SKIPPED** (config.trailers.enabled = False)
  - DISPATCH: ~15 items à déplacer vers disques
- Risques connus:
  - Disk2 à 93% — possibilité d'être sélectionné pour gros items à éviter
  - 30 stale entries dans matrix (à examiner hors run)

## Exécution

### INGEST — OK
- Output: `Ingest: 0 OK, 36 skipped, 0 errors`. Tous les hashes qBit déjà trackés.
- Durée: ~185ms (fast-path tracker-only).
- **Design Conformity Check**:
  - `torrents_found completed=36 total=36` → DESIGN_CONFORM
  - `ingest_complete skipped=36 success=0 errors=0` → DESIGN_CONFORM (idempotent re-run)
  - `.DS_Store` dans 097-TEMP → cosmétique, ignoré
- Agents techniques: clean (orphan-hunter), coherent (state-validator), normal (output-analyzer).
- Agent business (ingest-checker): 36/36 trackés, invariant AT OK, 0 miss.
### SORT — OK
- Output: `sort_fast_skip` + `Sort: 0 OK, 0 skipped, 0 errors`. Durée ~31ms.
- **Design Conformity Check**:
  - `sort_fast_skip ingest_dir='.../097-TEMP'` quand TEMP vide → DESIGN_CONFORM
- Agents techniques: clean (orphan-hunter, .DS_Store cosmetic only), coherent (state-validator), normal (output-analyzer).
- Agent business (sort-checker): 15/15 dans la bonne catégorie, 097-TEMP vide, 0 doublon.
### CLEAN — OK
- Output: `process_clean_complete recleaned=0 skipped=0 errors=0`. Pas de pollution.

### SCRAPE — OK
- Output: `Scrape: 1 OK, 13 skipped, 0 errors`. 1 rescrape (Top Chef Le Concours Parallèle), 4 movies + 9 TV shows skippés (NFO valides).
- **Design Conformity Check**:
  - Mikado `movie_no_tmdb_results` + `movie_db_restore_success` (4 fichiers copiés depuis /Volumes/Disk1) → DESIGN_CONFORM (TMDB Auto-Recovery via DB restore, matrix v2.1)
  - Top Chef Le Concours Parallèle: `show_rescrape_drift` (episode_naming_drift) → TVDB match confidence 1.0 → `provider_lock_engaged tvdb suppressed=tmdb` → DESIGN_CONFORM (provider-ids feature)
  - `show_season_empty season=17` + `episode_unmatched_no_rename` + `show_episodes_unmatched` → DESIGN_CONFORM (Unmatched Episode Policy — TVDB n'a pas S17, fichier laissé en place)
  - `artwork_exists_skip` × 3 → DESIGN_CONFORM (idempotent)
  - NFO check post-rescrape (scrape-checker): tous les 15 items avec TVDB/TMDB/IMDB IDs corrects, posters + landscapes présents
- Agent technique: clean (orphan-hunter, invariants AD/AE/AF OK), coherent (state-validator), une déviation non-documentée par output-analyzer (voir DEVIATION LIST #1).
- Agent business (scrape-checker): 15/15 PASS, 0 deviation.

### CLEANUP — OK
- Output: `process_cleanup_complete removed=0`. Aucun fichier parasite.
### ENFORCE — OK
- Output: `Enforce: 0 fixed, 15 OK, 0 errors`. 15 structure_ok + 15 coherence_ok.
- **Design Conformity Check**: tous les events `enforce_*` documentés dans matrix v2.1.
- Agent technique: clean (orphan-hunter), output-analyzer CLEAN, state-validator → faux positif (TOOLING_BUG #2 ci-dessous).
### VERIFY — OK (14/15)
- Output: `Verify: 14 OK, 1 blocked; 14 ready for dispatch`. 
- **Design Conformity Check**:
  - 5 movies `status=valid` (Mikado 11/12, autres 12/12)
  - 9 TV shows `status=valid` (17/18 systémique)
  - 1 TV show `status=blocked` (Top Chef Le Concours Parallèle, 15/18): `'No Saison XX/ with properly named episodes', 'Unrenamed episodes: Top.Chef.Le.Concours.Parallele.S17E10...'` → DESIGN_CONFORM (Unmatched Episode Policy safety net, matrix v2.1 §VERIFY explicite)
- Agents techniques: clean (orphan-hunter), output-analyzer a relevé matrix-stale sur checks_total → DEVIATION #3 (mineur, enrichissement matrix).
### TRAILERS — SKIPPED (config.trailers.enabled = False)
### DISPATCH — STOPPÉ AVANT EXÉCUTION (HARD GATE C bloqué)
- Plan dry-run validé : 14 OK, 0 skipped, 0 errors. Top Chef Le Concours Parallèle correctement exclu (status=blocked, Unmatched Episode Policy).
  - disk_1: 11 items (5 moved-films, 2 replaced-films, 4 merged-séries — incluant Dexter New Blood, Imperfect Women, Maximum Pleasure Guaranteed, Stranger Things Tales en moved)
  - disk_2: 2 items (American Dad! merged, The Boys merged)
  - disk_3: 1 item (FROM merged, avec case-insensitive match design-conform : disk a 'From (2022)', staging a 'FROM (2022)')
  - disk_4: 0 items
- **Design Conformity Check**: 1 case-insensitive match staging↔disk (FROM→From) → DESIGN_CONFORM (matrix §Dispatch Case-Insensitive Matching).
- **HARD GATE C résultat** : BLOQUÉ par rule 11 (3 DEVIATIONS à `À TRAITER`, toutes mineures).
- **Décision opérateur** : `Stopper le pipeline ici`. Pas de real dispatch.

## DEVIATION LIST

Anormal vs design + tooling/operational. DESIGN_CONFORM lives in the step's "Design Conformity Check" section, not here.

| # | Category | Severity | Step | Description | Status |
|---|----------|----------|------|-------------|--------|
| 1 | DESIGN_DEVIATION | mineur | PROCESS:scrape | Event `item_issue_persist_skipped_no_item path='.../Top Chef Le Concours Parallèle (2026)'` émis avant scrape réussi. Le log a été ajouté en commit 0916232 (`fix(scraper): log missing DB in DriftIssueStore.from_config`) mais l'événement n'est pas documenté dans matrix v2.1 §PROCESS:scrape. Comportement intentionnel (DriftIssueStore skip silent quand item DB absent pré-match) ; nécessite enrichissement matrix. | À TRAITER |
| 2 | TOOLING_BUG | mineur | ENFORCE | Agent `pipeline-state-validator` a faussement signalé "100% phantom ratio" en post-ENFORCE. L'agent supposait que `ingested_torrents.json` contenait `dest_path` ou des noms de dossiers, alors que la structure réelle est `{hash: {name, action, date}}` (clé = hash qBit, name = torrent name brut). Aucun problème réel. Le prompt de l'agent doit être affiné pour comprendre la sémantique tracker (déjà vérifié par `pipeline-ingest-checker` qui confirme 36/36 matches). | À TRAITER |
| 3 | DESIGN_DEVIATION | mineur | VERIFY | Matrix v2.1 §VERIFY ne documente pas les `checks_total` par type d'item. Observé : films = 12 checks (Mikado 11/12 — probablement `-landscape.jpg` absent, DESIGN_CONFORM côté scrape-checker mais comptabilisé comme check raté), TV shows déjà-dispatchés = 17/18 systématiquement (1 check skippé constant — candidat : check library DB ou check de cohérence post-dispatch). Pattern non bloquant (statut reste `valid`) mais empêche la traçabilité matrix. Enrichissement matrix nécessaire. | À TRAITER |
| 4 | **ACCEPTANCE_FAIL** | **critique** | BDD (PHASE 3) | **provider-ids ACCEPTANCE #4 violation — PERSISTANTE depuis run 2026-05-25-09h57**. 167 items avec `canonical_provider` inversé (TV→tmdb au lieu de tvdb, movies→tvdb au lieu de tmdb) + `external_ids_json` vide. Run précédent avait 209 shows + 142 movies. Le nombre a diminué (probable correction partielle entre runs) mais la régression persiste. Impact : downstream dépendant de `canonical_provider` (backfill épisodes, fallback ranking) compromis. **Doit avoir un test de régression** (memory rule "test de régression par bug"). | À TRAITER (carry-over) |
| 5 | DESIGN_DEVIATION | mineur | BDD (AO, AG, AJ, AR) | 4 invariants violés (mineurs) : **AG** 4093 NFO sans vidéo parente sur disques (legacy ou stale) ; **AJ** 28+ `.actors/` dirs résiduels sur tous les disques ; **AO** 102 `media_file` sans `release_id` (confirme `library-reconcile.files_without_release=102`) ; **AR** schema `repair_queue` sans colonne `created_at` (schema drift vs invariant). Aucun bloque l'exécution mais pollue la BDD/disques. | À TRAITER |
| 6 | DESIGN_DEVIATION | mineur | BDD (PHASE 3) | `library-reconcile` report : 172 release_orphans (releases sans media_item parent). Signal nouveau non couvert par les invariants AD-AV. Probable nettoyage de cohérence requis. | À TRAITER |
| 7 | TOOLING_BUG | mineur | PHASE 3 / invariant-checker | Invariant **AT** (qBit hashes match tracker) reporté SKIP "qBit unreachable" alors que qBit était joignable en PHASE 0 (`personalscraper torrents-list` a réussi). L'agent invariant-checker a un faux négatif de connectivité. | À TRAITER |

Categories: DESIGN_DEVIATION / ACCEPTANCE_FAIL / TOOLING_BUG / OPERATIONAL.
Severity: critique / majeur / mineur.

## Diff vs run précédent (2026-05-25-09h57)

| Métrique | Run précédent | Ce run | Δ |
|---|---|---|---|
| Status | STOPPÉ AVANT DISPATCH | STOPPÉ AVANT DISPATCH | identique |
| Items en staging | 13 valid + 2 blocked | 14 valid + 1 blocked | +1 valid (Mikado récupéré par Auto-Recovery) |
| ACCEPTANCE_FAIL provider-ids #4 | 209 shows + 142 movies inversés | 167 items inversés | -184 (correction partielle ?) |
| DEVIATIONS critique | 1 | 1 | identique (même classe : provider-ids) |
| DEVIATIONS majeur | 4 | 0 | -4 (resolved entre runs) |
| DEVIATIONS mineur | 6 | 6 | identique mais classes différentes |
| Invariant AG (NFO sans vidéo) | non testé explicite | 4093 violations | +4093 (nouveau check de l'agent) |
| Invariant AJ (.actors/ residue) | tous OK | 28+ violations | +28 |
| Invariant AO (release_id NULL) | 102 | 102 | identique persistant |
| library-reconcile release_orphans | non rapporté | 172 | nouveau signal |

**Patterns persistants** (mêmes bugs sur 2 runs consécutifs) :
- ACCEPTANCE_FAIL provider-ids #4 (canonical_provider inversé)
- Invariant AO (102 media_file sans release_id)
- Top Chef Le Concours Parallèle S17E10 toujours bloqué (Unmatched Episode Policy, TVDB n'a pas la S17)

**Résolus entre runs** :
- VERIFY émettait 0 structlog events au run précédent (DEV #94) → ce run les émet normalement.
- Matrix-aware agents discoverability (matrix v2.0 → v2.1 bump avec inventaire correct).

## Analyse finale

**Status global** : STOPPÉ AVANT DISPATCH (read-only). Pipeline fonctionnellement saine côté staging (15 items scrapés correctement, 14 verifiés OK), mais la BDD `library.db` présente des défauts structurels persistants qui violent le contrat provider-ids.

**Findings critiques** :
1. **DEV #4 (ACCEPTANCE_FAIL critique)** : provider-ids ACCEPTANCE #4 violation (167 items inversés). Régression confirmée sur 2 runs consécutifs. Bloque la confiance dans le système de canonical_provider. **Nécessite** :
   - Investigation racine (quand/comment les rows ont été insérées)
   - UPDATE SQL massif avec backup BDD préalable
   - Test de régression (per memory rule "test de régression par bug")

**Findings non-critiques mais persistants** :
2. **DEV #5 (invariants AG/AJ/AO/AR)** : pollution BDD/disques (NFO sans vidéo parente, .actors/ residue, media_file sans release_id, schema repair_queue). Tous mineurs, à traiter en nettoyage groupé.
3. **DEV #6 (release_orphans)** : 172 releases sans parent — nouveau signal `library-reconcile`. Investigation nécessaire pour comprendre l'origine.

**Patterns systémiques** :
- BDD désynchronisée vs état réel staging (Mikado item 518 a sauvé un scrape grâce au DB restore, mais le `canonical_provider` y est probablement inversé aussi).
- Le pipeline lui-même (staging→scrape→verify→dispatch dry-run) est sain. Le défaut est en aval (BDD coherence).

**Stale-detector (PHASE 0)** : 30 stale events dans matrix v2.1 + 369 coverage gaps (events code non documentés). Recommandation : bump matrix vers v2.2 pour intégrer les events de recovery, cleanup, trailers, TMDB validation (cf. report PHASE 0).

**Recommandations post-run** :
- Lancer `/implement:feature` pour traiter DEV #4 (provider-ids correction + test de régression). **P0**.
- Nettoyage BDD/disques (DEV #5, #6) en session dédiée. **P1**.
- Enrichissement matrix v2.2 (DEV #1, #3 + stale events). **P2**.
- Investigation TOOLING_BUGs (DEV #2, #7) — affiner prompts des agents. **P2**.

## Traitement

Filled by PHASE 4 only when `--remediate` flag was passed. **Mode: read-only**, so PHASE 4 is a no-op. Run `/implement:feature` pour adresser les findings P0.
