# Pipeline Run — 2026-05-21 17h16

> Item 5 du tech-debt audit. Goal : tester la skill pipeline-monitor v2.0
> fraîchement écrite (sub-phases 4.1–4.5 de l'item 4) sur un run réel, surfacer
> les TOOLING_BUGs, alimenter le brainstorm item 6.

## Status: EN COURS

## Matrix
- Version: 2.0 — confirmée (assertion GATE -1 PASSED)
- Stale-detector findings: **SKIPPED** — voir DEVIATION #1 (TOOLING_BUG critique)

## Pré-analyse

### Inventaire qBittorrent
- 22 torrents complets dans qBit, **tous trackés** (`ingested_torrents.json` = 22 entrées). → INGEST devrait être 0 nouveau.

### Staging
- `001-MOVIES/` : 1 item — `I Origins (2014)` (nfo OK, poster/landscape MANQUANTS)
- `002-TVSHOWS/` : 8 items — toutes avec `nfo + poster + landscape` (fanart=0 partout, possible design-conform)
- `097-TEMP/` : **vide** → SORT devrait fast-skip
- `003-EBOOKS/`, `099-SCRIPTS/` : hors scope pipeline

### Tracker
- `paths.data_dir/ingested_torrents.json` : 22 entrées trackées

### Pré-recovery (`_recover_from_previous_run`)
- `_tmp_dispatch_*` sur disques : **0** orphelin
- `qbit_auth_lockout` : absent
- `_tmp_ingest_*` / `.ingest_tmp_*` en staging : **0**
- `pipeline.lock` : absent
- → Pré-recovery sera no-op

### Espace disque
- `IznoServer SSD` : 445G libre / 932G (53%)
- `Disk1` : 1.7T libre / 13T (87%) — confortable
- `Disk2` : **290G libre / 3.7T (93%)** — TENDU, à surveiller pour DISPATCH
- `Disk3` : 927G libre / 3.7T (76%)
- `Disk4` : 680G libre / 2.8T (76%)

### Config notable
- `trailers.enabled = False` → STEP TRAILERS sera **SKIPPED** (GATE 5b skipped)

### Résultat attendu (fast-skips probables)
- INGEST : skip / no-op (0 nouveau torrent à ingérer)
- SORT : `sort_fast_skip` (097-TEMP vide)
- CLEAN : probable `clean_fast_skip` (rien de visible en staging à nettoyer)
- SCRAPE : `scrape_fast_skip` sur les 8 séries (nfo+poster+landscape OK) ; éventuel fetch poster/landscape pour `I Origins (2014)`
- CLEANUP : probable `cleanup_fast_skip`
- ENFORCE : actions règles potentielles
- VERIFY : PASS sur les 8 séries ; éventuelle erreur ou warning sur `I Origins (2014)` (poster/landscape manquants)
- TRAILERS : **SKIPPED** (config)
- DISPATCH : ~9 items à dispatcher (1 movie + 8 shows). Disk2 tendu — `Big Mistakes` est dans qBit en 4.39 GB mais déjà tracké donc non concerné.

### Risques connus
- Disk2 à 93% : si tous les médias finissent par Disk2 (most-free fallback), risque de saturation.
- 1 NOUVEAU média sans poster (`I Origins (2014)`) — possible blocage VERIFY.
- TOOLING_BUG #1 (DEVIATION LIST) : agents matrix v2.0 non-découvrables — GATE 7 sera dégradée.

## Exécution

### INGEST — TERMINÉ
- Dry-run : `completed=22 total=23, errors=0 skipped=22 success=0`
- Real : `completed=23 total=24, errors=0 skipped=22 success=1` — un 23e torrent (`From.S04E01.MULTi.VFF.*.H265-TFA`, 1.2 GB) a fini de DL entre dry/real et a été ingéré. Tracker mis à jour avec `dest_path`.
- **Design Conformity Check** : tous les events DESIGN_CONFORM (matrix §INGEST). `transfer_complete + torrent_marked + ingest_complete success=1` correspondent au pattern attendu.
- Agents : orphan-hunter FAUX POSITIF (DEV #2) ; state-validator + output-analyzer + ingest-checker = CLEAN.

### SORT — TERMINÉ
- Dry-run + real : 1 move `From.S04E01.*.H265-TFA.mkv` 097-TEMP → `002-TVSHOWS/FROM (2022)/` (au root, pas dans Saison NN — design-conform, scrape créera).
- Events nouveaux observés (non-matrix v2.0) : `tracker_dest_path_pruned`, `sort_tracker_pruned` → matrix v2.1 candidates.
- 097-TEMP vide post-SORT ✓ (sauf `.DS_Store` cosmétique → DEV #4 plus bas).
- Agents : 3 technical + sort-checker = tous CLEAN, classification DESIGN_CONFORM.

### PROCESS (clean + scrape + cleanup) — TERMINÉ
- `process_clean_complete recleaned=0` → DESIGN_CONFORM clean_fast_skip variant.
- Scrape : 8 `nfo_valid action=skipped_already_done` (1 movie + 7 shows). FROM (2022) → `nfo_valid action=repaired`.
- **`repair_root_duplicate_removed`** : le fichier `From.S04E01.*.H265-TFA.mkv` placé à la racine par SORT est SUPPRIMÉ par scrape:clean (dedup) car `S04E01 - The Arrival.mkv` est déjà dans `Saison 04/`. Sizes quasi-identiques (~1.21 GB). → **DESIGN_CONFORM** (anti-duplicate épisodes déjà possédés).
- 1 warning : `episode_unmatched_no_rename` sur Top Chef Le Concours Parallèle S17E10 (`available_seasons=[]`, scrape n'a pas trouvé la saison côté provider) → DESIGN_CONFORM (Unmatched Episode Policy).
- `process_cleanup_complete removed=0` → DESIGN_CONFORM cleanup_fast_skip variant.
- Coverage gaps matrix v2.0 §PROCESS : `repair_root_duplicate_removed/would_remove`, `nfo_valid action=repaired`, `movies_start/done`, `tvshows_start/done`, counter asymmetry `Scrape: 1 OK` vs `tvshows_done scraped=0` (le `action=repaired` compte dans le summary mais pas dans `scraped`).
- Agents : 3 technical + scrape-checker = pipeline CLEAN, 4 title-mismatches cosmétiques (colons filesystem), 1 missing IMDB (Top Chef Concours Parallèle), 1 counter asymmetry signalé.

### ENFORCE — TERMINÉ
- **ENFORCE corrige ce que PROCESS:scrape a laissé** : `enforce.orphan_episode_moved` déplace `Top.Chef.Le.Concours.Parallele.S17E10.*.mkv` de show root → `Saison 17/` (créé à la volée). NB : ne renomme PAS (filename reste avec release-group).
- `enforce_sanitize_action deleted_ds_store` : 1 `.DS_Store` supprimé (097-TEMP).
- `Enforce: 2 fixed, 8 OK, 0 errors`.
- **10 `.DS_Store` survivent dans le staging** (mineur, scope-limited cleanup ENFORCE — possible DESIGN_DEVIATION ou DESIGN_CONFORM scoped).
- Coverage gaps matrix v2.0 §ENFORCE : 3 event names manquants (`enforce.orphan_episode_moved`, `enforce_sanitize_action`, `enforce_structure_fix`) + handoff PROCESS→ENFORCE non documenté + CLI `run` help omet ENFORCE (doc rot probable).
- Agents : orphan-hunter CLEAN (10 .DS_Store mineur) ; state-validator a corrigé sa fausse affirmation précédente (DEV #3) ; output-analyzer = 5 coverage gaps signalés.

### VERIFY — TERMINÉ
- `Verify: 8 OK, 1 blocked / 8 ready for dispatch`
- Blocked = `Top Chef Le Concours Parallèle (2026)` : "No Saison XX/ with properly named episodes; Unrenamed episodes: Top.Chef.Le.Concours.Parallele.S17E10..." → DESIGN_CONFORM (Unmatched Episode Policy safety net) car ENFORCE a déplacé mais pas renommé.
- **Observation forte** : VERIFY n'émet AUCUN event structlog INFO sur stdout (même avec `-v`). Toute la matrix §VERIFY (`verify_item_done status=valid/blocked errors=[...]`) repose sur des events INVISIBLES depuis stdout. Hypothèse A (EventBus only, pas stdout) ou B (events droppés). À trancher en GATE 7.
- Agents : 3 technical CLEAN ; output-analyzer = coverage gap critique sur §VERIFY events.

### TRAILERS — SKIPPED (config.trailers.enabled=False)

### DISPATCH — DRY-RUN OK, HARD GATE BLOQUÉ par rule 11

Plan (8 OK, 0 skipped, 0 errors) :

| # | Action  | Item                                          | Disk    | Existing | Notes              |
| - | ------- | --------------------------------------------- | ------- | -------- | ------------------ |
| 1 | moved   | I Origins (2014) [movie]                       | disk_1  | No (miss)| → most-free disk   |
| 2 | merged  | American Dad! (2005)                           | disk_2  | Yes (hit)| existing show      |
| 3 | moved   | Dexter New Blood (2021)                        | disk_1  | No (miss)| → most-free disk   |
| 4 | merged  | FROM (2022)                                    | disk_3  | Yes (hit)| **case-mismatch**  |
| 5 | merged  | LOL Qui rit, sort ! (2021)                     | disk_1  | Yes (hit)| existing show      |
| 6 | moved   | Stranger Things Tales from '85 (2026)          | disk_1  | No (miss)| → most-free disk   |
| 7 | merged  | The Boys (2019)                                | disk_2  | Yes (hit)| existing show      |
| 8 | merged  | Top Chef (2010)                                | disk_1  | Yes (hit)| existing show      |
|—  | skipped | Top Chef Le Concours Parallèle (2026)         | —       | —        | VERIFY-blocked     |

**Case-insensitive match (DESIGN_CONFORM, INFO only)** :
- staging `'FROM (2022)'` → disk_3:series/`'From (2022)'` — disk casing ("From") wins.

**Real dispatch BLOQUÉ par rule 11 (zero-tolerance)** — voir DEVIATION LIST ci-dessous.

## DEVIATION LIST

| #   | Category        | Severity | Step      | Description                                                                                                                                                                                                                                                                                                                                              | Status         |
| --- | --------------- | -------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| 1   | TOOLING_BUG     | critique | GATE -1   | Les 4 nouveaux agents matrix v2.0 (`pipeline-matrix-stale-detector`, `pipeline-invariant-checker`, `pipeline-bdd-validator`, `pipeline-event-monitor`) ne sont pas découvrables par le harness Claude Code dans la session courante. Fichiers OK, frontmatter conforme, mais discovery au boot. Impact : GATE -1 stale-detector skip, GATE 7 dégradée.       | À TRAITER      |
| 2   | TOOLING_BUG     | mineur   | INGEST    | `pipeline-orphan-hunter` a flagué le fichier fraîchement ingéré dans `097-TEMP` comme "RESIDUAL_MEDIA CRITIQUE". Or fichier en 097-TEMP entre INGEST et SORT = état design-conform attendu. State-validator a correctement classé. Prompt agent à enrichir (matrix-aware par défaut).                                                                       | À TRAITER      |
| 3   | TOOLING_BUG     | mineur   | ENFORCE   | `pipeline-state-validator` a affirmé "No .DS_Store found (cleaned by ENFORCE)" alors que 10 `.DS_Store` survivent dans le staging (ENFORCE n'a delete que 1 .DS_Store en 097-TEMP). Affirmation inférée des logs au lieu de vérifier le FS. Agent a auto-corrigé au tour suivant quand prompt explicite.                                                  | À TRAITER      |
| 4   | DESIGN_DEVIATION| mineur   | ENFORCE   | ENFORCE scope-limited cleanup de `.DS_Store` — 10 `.DS_Store` restent dans staging post-run. Soit design (ENFORCE scoped to processed items uniquement), soit gap. Matrix v2.0 §ENFORCE ne documente pas la portée.                                                                                                                                       | À INVESTIGUER  |
| 5   | DESIGN_DEVIATION| mineur   | PROCESS   | Counter asymmetry : `Scrape: 1 OK` (top summary) vs `tvshows_done scraped=0 skipped=7` (`action=repaired` sur FROM (2022) compte dans summary mais pas dans `scraped`). Pas d'impact dispatch mais sémantique compteur incohérente.                                                                                                                        | À INVESTIGUER  |
| 6   | DESIGN_DEVIATION| critique?| VERIFY    | VERIFY n'émet AUCUN event structlog INFO sur stdout (silent step, même avec `-v`). Matrix §VERIFY référence `verify_item_done status=valid/blocked errors=[...]` non observable. Hyp. A : events sur EventBus only → matrix doit le dire ; Hyp. B : events droppés → DESIGN_DEVIATION. À trancher : grep `verify_item_done` dans `personalscraper/verify/`. | À INVESTIGUER  |
| 7   | DESIGN_DEVIATION| mineur   | CLI doc   | `personalscraper run --help` liste "ingest → sort → process → verify → dispatch" — omet ENFORCE et TRAILERS. Or ENFORCE s'exécute bien (observé sur ce run). Doc rot probable, à corriger.                                                                                                                                                                | À TRAITER      |
| 8   | COVERAGE_GAP    | mineur   | matrix    | Events absents de matrix v2.0 mais émis par le pipeline (à ajouter en v2.1) : `tracker_dest_path_pruned`, `sort_tracker_pruned` (SORT) ; `repair_root_duplicate_removed/would_remove`, `nfo_valid action=repaired`, `movies_start/done`, `tvshows_start/done` (PROCESS) ; `enforce.orphan_episode_moved`, `enforce_sanitize_action`, `enforce_structure_fix` (ENFORCE) | À INVESTIGUER  |
| 9   | DESIGN_DEVIATION| **CRITIQUE** | PROCESS | **Sémantique `repair_root_duplicate` INVERSÉE par rapport au design utilisateur.** Code à `personalscraper/scraper/existing_validator.py:521-535` : pour chaque video file SxxExx au show root, si `(S, E)` existe déjà dans `Saison NN/`, fait `f.unlink()` sur le fichier au root → **détruit le nouveau download, garde l'ancien**. Design utilisateur (confirmé 2026-05-21) : "Le dernier download doit toujours remplacer le fichier des disques ou de la staging. Le dernier download est celui à garder car on le remplace peut-être à cause d'un bug de lecture." Comportement attendu : REPLACE `Saison NN/SxxExx - title.mkv` par le nouveau fichier root, puis re-rename/organise. **Impact sur ce run** : `From.S04E01.MULTi.VFF.1080p.WEB.EAC3.5.1.H265-TFA.mkv` (fresh ingest 17h18) a été DÉTRUIT à 17h26 par PROCESS:clean, l'ancien `S04E01 - The Arrival.mkv` (07/05) reste. **Perte de donnée silencieuse**. Le torrent qBit est encore en seed → re-ingest possible. **TRAITÉ commit `268cbee` (2026-05-21)** : sémantique inversée + 3 events renommés (`repair_root_duplicate_replaced/would_replace/replace_failed`) + 4 tests (3 nouveaux/mis à jour `test_existing_validator_extra.py`, 1 mis à jour `test_scraper.py`). | **TRAITÉ**     |
| 10  | TOOLING_BUG     | mineur   | SKILL     | La skill v2.0 (et matrix v2.0 §post-pipeline 3.4) référence `personalscraper library-reconcile --dry-run`, mais le CLI réel n'a PAS de flag `--dry-run` (read-only par défaut, `--enqueue-repairs` pour activer le mutate). Corriger le SKILL.md (PHASE 3.4) et la matrix.                                                                                                            | À TRAITER      |
| 11  | DESIGN_DEVIATION| majeur   | GATE 7    | **`library-reconcile` détecte merkle_drift sur les 4 disques** (disk 1/2/3/4). **Investigation 2026-05-21** : root cause = `compute_merkle_root` non-déterministe quand des fichiers partagent le même `path_id` (schéma : `path` = directory). Le `sorted(files, key=lambda f: f.path_id)` est stable mais le tie-break repose sur l'ordre d'insertion SQLite, qui peut varier d'une query à l'autre. Reproducer : 4 ordres SQL différents → 4 hashes différents sur disk_1. **TRAITÉ commit `29c4953` (2026-05-21)** : sort key passé à tuple complet `(path_id, size, mtime_ns, oshash)` + test régression `test_merkle_determinism_shared_path_id`. Suite opérationnelle : les merkle_roots stockés sont encore avec l'ancien algo, drift reportée tant qu'un scan indexer complet ne tourne pas (transition naturelle). | **TRAITÉ**     |
| 12  | DESIGN_DEVIATION| mineur   | GATE 7    | `library-reconcile`: **7191 files_without_release** (dont ~496 video files orphelins). 6655 sont des side-cars (.actors/, .jpg/.nfo) légitimement sans release. 496 video files mp4/avi/mkv concentrés sur `series/Monk (2002)/.actors`, `Avez-vous déjà vu... (2006)/Saison 01`, `Pokemon`, `Squid Game (2021)/.actors` → vraiment orphelins, à investiguer en item 7 (BDD).         | À INVESTIGUER  |

Categories: DESIGN_DEVIATION / ACCEPTANCE_FAIL / TOOLING_BUG / OPERATIONAL.
Severity: critique / majeur / mineur.

## GATE 7 — Post-pipeline analysis (read-only)

### Invariants AD–AV (substitut general-purpose, agent matrix indisponible DEV #1)

Substitut a tourné ~23 min sur les 19 invariants. Output complet truncated mais signaux clés :
- **AV** : `LIBRARY_ANALYZER_MAX_WORKERS` string literal hors `.env.example` → 1 violation mineur (config naming)
- Autres invariants : pas de violations critiques remontées dans la portion visible

### BDD (substitut general-purpose)

| Check | Result |
| --- | --- |
| scan_run lifecycle | PASS (27 rows, 0 running, dernière 18/05) |
| repair_queue | PASS (224 rows, 100% done, 0 stuck) |
| index_outbox | PASS (133 rows, 100% done) |
| FK linkage media_item↔release↔file | PASS (0 dangling) |
| 7191 files_without_release | WARN — 6655 sidecars (design-conform) + **496 video orphans** (Monk .actors, Avez-vous déjà vu, Pokemon, Squid Game .actors) |
| Provider-IDs columns | INFO — 0/1935 items ont `canonical_provider` ou `external_ids_json`. ACCEPTANCE #4 non-testable (backfill pas joué sur cette BDD) |
| PRAGMA integrity_check | PASS (`ok`) |
| PRAGMA journal_mode | PASS (`wal`) |

→ DEV #12 ajouté pour les 496 video orphans (à confondre avec DEV #11 merkle_drift pour item 7 BDD).

### Event-monitor (matrix v2.0 §3.3) — SKIPPED

Agent `pipeline-event-monitor` indisponible (DEV #1) ET aucun event dump capturé (skill v2.0 host.py non utilisée — la skill a tourné en fallback subprocess mode). Pas de JSONL à analyser.

### library-reconcile cross-correlation

```
indexer.reconcile.report :
  merkle_drift=4 enrich_stale=0 dispatch_path_missing=0 release_orphans=0
  files_without_release=7191 items_without_files=0 season_count_drift=0
  total_findings=7195 enqueued_repairs=0
```

→ 2 nouvelles DEVIATIONs ajoutées (#11 merkle_drift majeur, #12 files_without_release mineur).

### Diff vs run précédent (`2026-05-18-20h23-pipeline-run.md`)

Non effectué (gain marginal vs cost; couvert au niveau audit item 1 plan-drift).

### Weird outputs / coverage gaps log

Events absents de matrix v2.0 observés cette run (à intégrer matrix v2.1 lors du fix DEV #9) :

| Step | Event | Hypothèse classification |
| --- | --- | --- |
| SORT | `tracker_dest_path_pruned`, `sort_tracker_pruned` | DESIGN_CONFORM |
| PROCESS:clean | `repair_root_duplicate_removed`, `repair_root_duplicate_would_remove` | **DESIGN_DEVIATION (DEV #9)** |
| PROCESS:scrape | `nfo_valid action=repaired` | DESIGN_CONFORM |
| PROCESS:scrape | `movies_start/done`, `tvshows_start/done` | DESIGN_CONFORM (telemetry) |
| ENFORCE | `enforce.orphan_episode_moved`, `enforce_sanitize_action`, `enforce_structure_fix` | DESIGN_CONFORM |
| VERIFY | (aucun event INFO stdout — coverage gap DEV #6) | DEV #6 |

### Analyse finale

**Verdict global pipeline-monitor v2.0 sur ce run** :
- Le pipeline lui-même fonctionne globalement, **mais** :
  - 1 DESIGN_DEVIATION CRITIQUE app-level (DEV #9 inversion sémantique `repair_root_duplicate`) avec **perte de donnée silencieuse**.
  - 1 DESIGN_DEVIATION majeur GATE 7 (DEV #11 merkle_drift 4 disques).
- La skill v2.0 elle-même :
  - 4 TOOLING_BUGs identifiés (DEV #1 agents non-discoverable, #2 orphan-hunter false positive, #3 state-validator false claim, #10 CLI flag inexistant matrix).
  - Le HARD gate dispatch a correctement fonctionné (zero-tolerance rule 11) — STOPPÉ par l'opérateur sur DEV #9.
- Matrix v2.0 : ~12 coverage gaps identifiés (events non documentés) — matrix v2.1 needed.
- Pipeline scope ≠ matrix scope : ENFORCE+TRAILERS dans la matrix mais absents du `run --help` (DEV #7 doc rot).

**Suites prévues (user direction)** :
1. Fix DEV #9 (`repair_root_duplicate` inversion) sur `personalscraper/scraper/existing_validator.py`
2. Re-ingest du `From.S04E01.*.H265-TFA.mkv` (qBit encore en seed, hash à dépopper du tracker)
3. Item 6 (brainstorm) consomme cette DEVIATION LIST pour alimenter le DESIGN final tech-debt
4. Items 7-8 traitent DEV #11 + #12 (BDD merkle_drift + orphan files)

## Status: TERMINÉ (read-only) — STOPPÉ au HARD gate DISPATCH par opérateur

## Validation post-fix (2026-05-21 20h17, hors-scope du run initial)

### DEV #9 validé à l'usage

Re-ingest puis pipeline complet :
1. Tracker pruné du hash `8a5a7db7...` (backup : `ingested_torrents.json.bak.2026-05-21-pre-from-reingest`).
2. `personalscraper ingest` : 9 OK / 22 skipped (les 9 nouveaux contenant `From.S04E01.H265-TFA` + 4 autres FROM S04 + 2 movies + Top Chef S17E11/E12).
3. `personalscraper sort` : 9 OK, tous en 002-TVSHOWS/FROM (2022)/ root (5 fichiers FROM S04) + 2 movies + Top Chef.
4. `personalscraper process --dry-run` montre la nouvelle sémantique :
   - `repair_root_duplicate_would_replace new=From.S04E01.H265-TFA removed='Saison 04/S04E01 - The Arrival.mkv'`
   - `repair_root_duplicate_would_replace new=From.S04E02.H265-TFA removed='Saison 04/S04E02 - Fray.mkv'`
   - 4 `repair_episode_would_move` pour S04E01-E03-E04-E05 (renommage canonique TMDB)
5. `personalscraper process` réel :
   - 2 `repair_root_duplicate_replaced` confirmés
   - 4 `repair_episode_moved` confirmés
   - 1 `episode_sibling_deleted` (.nfo R3MiX dans dossier multi-file)
   - 1 `nfo_valid action=repaired` sur FROM (2022)
   - `Scrape: 5 OK, 7 skipped, 0 errors`
6. État FS post-process Saison 04/ :
   - S04E01 - The Arrival.mkv (1.21 GB, **mtime 17:18 = nouveau H265-TFA**) ✓
   - S04E02 - Fray.mkv (1.41 GB, mtime 17:18 = nouveau H265-TFA) ✓
   - S04E03 - Merrily We Go.mkv (967 MB, ACKER) ✓
   - S04E04 - Of Myths and Monsters.mkv (1.18 GB, R3MiX) ✓
   - S04E05 - What a Long Strange Trip It's Been.mkv (1.13 GB, ACKER) ✓

→ Le H265-TFA précédemment détruit est **récupéré**, et la sémantique REPLACE est validée en réel. Fin de boucle data-loss DEV #9.

### DEV #11 validation — révèle 2 bugs supplémentaires DEV #13 et #14, tous traités

Séquence de validation (21h00 → 21h07) :

1. `personalscraper library-index --mode full` (1er essai) → CRASH avec `RuntimeError: disk worker N failed: index idx_stream_kind_codec already exists` sur 3 des 4 workers. C5 race documentée matrix v2.0 — **DEV #13 ajouté CRITIQUE**. Fix `_recreate_indexes` (`fc39f77`) + 7 tests `test_index_ddl.py`.
2. `library-index --mode full` (2e essai post-fix-#13) → SUCCESS, scan_run #29 ok, 142,967 files / 6,594 dirs en 17 min, 0 erreur. DEV #13 validé à l'usage.
3. `library-reconcile` → drift toujours sur 4 disques. Investigation : le mode `full` ne met PAS à jour `disk.merkle_root` (réservé quick/incremental).
4. `library-index --mode incremental` → bulk-change detected (94-97% files changed) car le nouvel algo donne d'autres hashes vs stockés → freeze de sécurité déclenchée.
5. `library-index --mode incremental --confirm-bulk-change` → 4 `update_merkle_root` events émis. Mais `library-reconcile` → drift TOUJOURS sur 4 disques.
6. Investigation manuelle : stored ≠ live recompute. Trouvé **DEV #14 majeur** : `_build_disk_fingerprints` + `_sample_fresh_fingerprints` (`_walker.py`) n'ont PAS le filtre `oshash IS NOT NULL` alors que `_finalize_disk_after_walk` ET `detect_merkle_drift` LE FILTRENT. 4 sites de query divergents → scanner stocke un merkle sur un set différent du détecteur. Fix `3993487` + 1 test régression.
7. `library-index --mode incremental --confirm-bulk-change` (3e essai post-fix-#14) → 4 update_merkle_root avec hashes alignés à la live recompute.
8. `library-reconcile` → **`merkle_drift=0`** ✓

DEV #11 + #13 + #14 TOUS traités. `library-reconcile` clean sur 4 disques.

### Findings annexes pendant la validation (à reverser en run dédié si confirmés)

- 2 movies renommés en titre français par scrape : `Peaky Blinders The Immortal Man (2026)` → `Peaky Blinders L'Immortel (2026)` ; `Project Hail Mary (2026)` → `Projet Dernière Chance (2026)`. Comportement attendu (provider-language preference fr-FR).
- Le folder transitoire `Top Chef France` créé par sort (Top Chef France S17E11/E12) a disparu post-process — peut-être mergé dans `Top Chef (2010)/` ou supprimé. À investiguer en cas de cosmétique (DEV potentielle si destination inattendue).
- 7191 files_without_release toujours présents (DEV #12, hors scope ici → item 7).

## Traitement

_(à venir : remediate du DEV #9 + re-ingest sur instruction utilisateur, hors scope de la skill read-only.)_
