# Pipeline Run — 2026-06-03 17h36

## Status: TERMINÉ — 3 déviations CORRIGÉES (phases 18-20, PR #36). Pipeline prêt à reprendre.
INGEST + SORT exécutés (réels, OK). PROCESS arrêté au dry-run (rule 12) → 3 déviations détectées, toutes TRAITÉES:
DEV #1 (sample/RAR, phase 18), DEV #2 (TVDB alias, phase 19), DEV #3 (FK orphans, phase 20 + remédiation live).
Commits 520c01a7 / 6bf44386 / 5129bcb5 / a47fbe2e (gate). make test 6243✓, make check 91.33%✓, design-gaps 0✓.
DEVIATION LIST: zéro entrée non-clôturée → la gate dispatch (rule 11) n'est plus bloquée par ces déviations.
Gates: -1✓ 0✓ 1✓ 2✓ | 3 (process à relancer en réel) | 4-6 à exécuter | 7✓.

## Matrix
- Version: 2.5 (assertion PASSED)
- Skill: pipeline-monitor v2.5, read-only mode (no --remediate)
- 4 matrix-aware agents present in session inventory
- Stale-detector findings: 30 stale entries (WARNING — matrix maintenance due, not blocking).
  Clusters: 6 `recovery_*`, 6 TMDB-validation, 6 clean/cleanup sub-step, 3 TRAILERS,
  3 ENFORCE, 3 library-scan, 1 `external_ids_written`, 1 `show_match_failed`.
  1 test-only (`trailer_downloaded`). 344 coverage-gap candidates (out-of-scope, INFO).

## Pré-analyse
- qBit: 46 complétés (sur 46 trackés côté qBit)
- Tracker (ingested_torrents.json): 38 entrées
- Torrents à ingérer (estimation): ~8 (46 complétés − 38 trackés)
- Médias en staging: 5 films, 9 séries (déjà nommés/scrapés d'un run précédent)
  - 001-MOVIES: Fantômes contre fantômes (1996), Gourou (2026), La Cité des Anges (1998),
    Les Légendaires (2026), Projet Dernière Chance (2026)
  - 002-TVSHOWS: FROM (2022), Prescott (2026), Rick and Morty (2013), Scrubs (2026),
    Spider-Noir (2026), The Boys (2019), Top Chef (2010),
    Top Chef Le Concours Parallèle (2026), Widow's Bay (2026)
- Items dans 097-TEMP: 0 (vide)
- Pré-recovery: 0 dispatch-tmp, 0 ingest-tmp, lockout=false, pipeline.lock=absent
- Espace disque:
  - SSD (staging): 295 GB libre (69%)
  - Disk1: 1.7 TB libre (88%)
  - Disk2: 288 GB libre (93% — TENDU)
  - Disk3: 905 GB libre (76%)
  - Disk4: 680 GB libre (76%)
- Résultat attendu (fast-skips probables):
  - `sort_fast_skip` (097-TEMP vide)
  - `scrape_fast_skip` sur les 14 items déjà scrapés (NFO valides)
- Trailers: DÉSACTIVÉ (config.trailers.enabled=False) → STEP TRAILERS SKIPPED, GATE 5b SKIPPED
- Risques connus:
  - Disk2 à 93% (288 GB) — surveiller si gros médias dispatchés dessus
  - "Top Chef (2010)" = fallback TVDB 255968 DESIGN_CONFORM (convention bibliothèque),
    PAS un mismatch — ne pas re-flag

## Exécution

### INGEST — DONE (GATE 1 PASSED)
Real run: 8 OK, 38 skipped, 0 errors. 8 TV items copied → 097-TEMP, all marked in tracker (action=copied, dest_path set). Tracker now 46 entries. Duration ~4m25s.
8 ingested: Rafa S01E01(DOC)/E02/E03/E04, Lord of the Flies S01E01/E02, Murder Mindfully S01/S02.

**Design Conformity Check (INGEST):**
- `torrents_found completed=46 total=46` → DESIGN_CONFORM (qBit inventory)
- `transfer_complete action=copy dest=097-TEMP` ×8 → DESIGN_CONFORM (contract destination)
- `torrent_marked action=copied` ×8 → DESIGN_CONFORM (tracker write)
- `ingest_complete errors=0 skipped=38 success=8` → DESIGN_CONFORM (idempotence)
- Tracker `dest_path` on fresh entries → DESIGN_CONFORM (matrix INGEST contract line 74 lists dest_path).
  NOTE for GATE 7 / matrix maintenance: invariant AS describes schema as {name,action,date} WITHOUT
  dest_path — AS text vs INGEST contract are mutually inconsistent. Code is correct (follows contract).

**Agents:** orphan-hunter CLEAN · state-validator COHERENT (46 entries) · output-analyzer CLEAN (0 err/warn) ·
ingest-checker: 0 missed, 0 ingest-orphans, 8 copies size-verified (<1% rounding). No deviations.
### SORT — DONE (GATE 2 PASSED, 1 finding to investigate)
Real run: 8 OK, 0 skipped, 0 errors. All 8 → 002-TVSHOWS (Lord of the Flies, Murder Mindfully, Rafa).
`tracker_dest_path_pruned removed=8` + `sort_tracker_pruned removed=8`. 097-TEMP empty (.DS_Store only).

**Design Conformity Check (SORT):**
- `sort_item_moved` ×8, `sort_complete moved=8` → DESIGN_CONFORM
- `tracker_dest_path_pruned` / `sort_tracker_pruned` → DESIGN_CONFORM (matrix SORT v2.1)
- Show folders without year, episodes at show root (no Saison NN/) → DESIGN_CONFORM
- All 8 in correct category (002-TVSHOWS), 0 misclassified, 0 duplicates → DESIGN_CONFORM

**Agents:** orphan-hunter CLEAN · state-validator COHERENT · output-analyzer CLEAN · sort-checker 8/8 correct.

**FINDING (see DEVIATION #1):** Rafa S01E01.DOC/E02/E03/E04 contain ONLY multi-part RAR
archives (.rar/.r00-.r18/.sfv/.nfo), NO extracted video. Lord of the Flies + Murder Mindfully have
proper .mkv. Watching SCRAPE/VERIFY behavior.
### CLEAN — DRY-RUN DONE (HALTED before real run — see DEVIATION #1)
`process_clean_complete recleaned=0 errors=0`. **CLEAN did NOT remove the Rafa Sample/ folders**
(no sample-handling code exists anywhere in cleaner/process — grep confirmed). This is the root of DEV #1.
**Design Conformity Check:** `process_clean_complete recleaned=0` is DESIGN_CONFORM *as an event*, but the
absence of sample-stripping is a coverage gap (DEV #1).

### SCRAPE — DRY-RUN DONE (HALTED before real run — see DEVIATION #1, #2)
Dry-run summary: Scrape 3 OK, 14 skipped, 0 errors (movies 0/5 — all fast-skip; tvshows scraped=3, skipped=9, unmatched=1).

**Design Conformity Check (SCRAPE) — DESIGN_CONFORM:**
- 5 movies `nfo_valid skipped_already_done` → DESIGN_CONFORM (fast-skip)
- 8 TV shows `skipped_already_done` (FROM, Prescott, Rick and Morty, Scrubs, Spider-Noir, The Boys, Top Chef(2010), Widow's Bay) → DESIGN_CONFORM
- Top Chef(2010) skip → DESIGN_CONFORM (known TVDB-255968 fallback)
- Lord of the Flies: `show_tvdb_match` conf=1.0, `provider_lock_engaged tvdb 434004 suppressed tmdb`, `episode_would_rename` from real .mkv → Saison 01/Sxx - Piggy/Jack, artwork → DESIGN_CONFORM (clean scrape)
- Top Chef Le Concours Parallèle: `show_rescrape_drift` (episode_naming_drift S17E10), `item_issue_persist_skipped_no_item` (DESIGN_CONFORM line 219), `nfo_corrupt_rescrape_would_delete` (single file, rare → DESIGN_CONFORM line 217), `show_tvdb_match` conf=1.0, `provider_lock_engaged tvdb 475278`, `show_season_empty season=17` (DESIGN_CONFORM line 192), `episode_unmatched_no_rename S17E10 phantom_season=False` (DESIGN_CONFORM line 193 Unmatched Episode Policy), `show_episodes_unmatched` (line 194), `artwork_exists_skip` → DESIGN_CONFORM
- All `provider_lock_engaged` (tvdb primary, tmdb suppressed) → DESIGN_CONFORM (multi-provider separation)

**DEVIATIONS found in dry-run:**
- DEV #1 (MAJEUR): Rafa S01E01-E04 — scraper would rename the 34-47MB `Sample/*-sample.mkv` clips as the
  real episodes (`episode_would_rename source=...sample.mkv → Saison 01/S01E0x - Capitulo x.mkv`). Real video
  is in unextracted RAR. CLEAN didn't strip samples.
- DEV #2 (à investiguer): Murder Mindfully (16 real .mkv episodes) → `show_tvdb_match 'Achtsam Morden'
  conf=0.38` → `below_threshold` → `show_no_confident_match` → left unscraped. German original title vs
  English folder name. Findable show not matched.

### CLEANUP — DRY-RUN DONE
`process_cleanup_complete removed=0` → DESIGN_CONFORM (nothing to clean per its contract; note it does NOT
target Sample/ dirs either).
### ENFORCE — PENDING
### VERIFY — PENDING
### TRAILERS — SKIPPED (config.trailers.enabled=False)
### DISPATCH — PENDING

## DEVIATION LIST

Anormal vs design + tooling/operational. DESIGN_CONFORM lives in the step's
"Design Conformity Check" section, not here.

| # | Category | Severity | Step | Description | Status |
|---|----------|----------|------|-------------|--------|
| 1 | DESIGN_DEVIATION | MAJEUR | CLEAN/SCRAPE | Rafa S01E01-E04 = scene releases RAR + Sample/ clip matché comme épisode, aucune extraction RAR. | **TRAITÉ** (phase 18, commit 520c01a7 — extract RAR + strip sample + exclusion scrape + verify no_archive_files. Vérifié réel: extracted=3 sample_stripped=4, plus de source=...sample.mkv) |
| 2 | DESIGN_DEVIATION | à investiguer | SCRAPE | Murder Mindfully (16 épisodes) non matché (Achtsam Morden conf 0.38) — aliases/translations TVDB ignorés. | **TRAITÉ** (phase 19, commit 6bf44386 — parse dict translations + aliases, score best-of. Vérifié réel: 'Achtsam Morden' conf=1.0, 16 épisodes organisés) |
| 3 | DESIGN_DEVIATION | majeur | GATE 7 / BDD | library.db 11 FK orphans bloquent library-reconcile (IndexerFKOrphansError). PRÉ-EXISTANT. | **TRAITÉ** (phase 20, commit 5129bcb5 — détecteur + clean_fk_orphans cascade-aware + open tolérant. Remédiation live: 11 nettoyés, cascade 47+58, foreign_key_check=0, integrity ok) |

Categories: DESIGN_DEVIATION / ACCEPTANCE_FAIL / TOOLING_BUG / OPERATIONAL.
Severity: critique / majeur / mineur.

## GATE 7 — Post-pipeline analysis (read-only, on halted state)

### 3.1 Invariant checker (AD–AV)
12/19 pass. Violations (all PRE-EXISTING, not caused by this run's INGEST+SORT):
- **AN** (5 orphan media_release) → folded into DEV #3 (FK orphans).
- AG (2 orphan tvshow.nfo: New Girl, Trotro — no video in tree), AH (53 season-specials-poster.jpg w/o Saison 00/),
  AO (~60-242 media_file w/o release_id), AP (27 season episode_count drift), AQ (1307 has_poster flag stale)
  → **pre-existing library maintenance backlog (mineur)**, out of scope for this halted run.
- AS PASS (tracker schema OK, 46 entries; dest_path concern confirmed DESIGN_CONFORM — no matrix conflict tripped).
- AT qBit "unreachable" → TRANSIENT (reachable now, 46 completed) → NON REPRODUCTIBLE.
- AU/AV PASS.

### 3.2 BDD validator (library.db)
- integrity_check=ok, WAL clean, not locked, 60MB.
- scan_run: 6 stale `failed` rows (2026-05-03→05-23, old) — pre-existing historical records, no active running scans (AK ok).
- repair_queue: 88 pending (backlog, <100), 1220 done, none stuck — pre-existing.
- outbox: healthy (30 pending newest 3h old, 192 done).
- provider-ids: **25 TV canonical=tmdb → DESIGN_CONFORM** (TVDB-unknown fallback, agent self-corrected).
  **65 NULL canonical_provider → NOT an ACCEPTANCE_FAIL** (verified: author-name/ebook entries like "H. G. Wells",
  "Agatha Christie", external_ids_json={}; matches prior run's known "165 ID-less" backlog). Agent over-classification
  REJECTED (TOOLING_BUG of bdd-validator). ratings_json: 1470 set / 483 null (legit).

### 3.3 Event-monitor — SKIPPED (fallback subprocess mode, no event dump captured).

### 3.4 library-reconcile cross-correlation — FAILED
`library-reconcile --read-only` crashed: **IndexerFKOrphansError (11 FK orphans)** → see DEV #3. The reconcile
tool that would drain AN/AO/AP/AQ/repair_queue is itself BLOCKED by the FK orphans. Fixing DEV #3 unblocks the
whole maintenance chain.

### 3.5 Diff vs run précédent (2026-06-03-11h08)
- Prev run: STOPPÉ at dispatch gate (DEV: Rick & Morty split, name+year vs provider-ID mismatch). 0 dispatched.
- **Resolved since:** R&M split DEV is TRAITÉ (torrent-write phase 15 — dispatch matches by canonical provider ID).
  This run R&M (2013) showed `skipped_already_done`, no split → **deviation did NOT recur. Confirmed fixed.**
- **New this run:** DEV #1 (Rafa sample/RAR, majeur), DEV #2 (Murder Mindfully no-match).
- **Persisting:** none from prev run. (DEV #3 FK orphans is pre-existing but was not flagged in the 11h08 run since
  that run stopped before GATE 7.)
- Items dispatched: 0 (both runs halted before dispatch).

### 3.6 Weird outputs: none beyond the recorded DEVIATIONS.

## Analyse finale

Ce run a exécuté INGEST + SORT réels proprement (8 torrents TV → 097-TEMP → 002-TVSHOWS, 0 erreur, tracker
cohérent). Il s'est arrêté volontairement au dry-run PROCESS (rule 12) sur découverte de 2 déviations de feature,
sans rien dispatcher ni créer d'état corrompu sur disque.

**Bugs feature confirmés (ce run):**
1. **DEV #1 (majeur) — Gestion des scene releases RAR + Sample/ absente.** Deux gaps cumulés: (a) CLEAN/cleanup
   ne supprime pas les dossiers `Sample/` ni les fichiers `-sample.mkv` (aucun code "sample" dans le pipeline),
   (b) aucune extraction RAR. Conséquence: le scraper matche les clips sample (34-47MB) comme épisodes
   (`episode_would_rename source=...sample.mkv`), et la vraie vidéo (RAR ~2.5GB) reste non traitée. Si dispatché,
   la bibliothèque TV recevrait des samples étiquetés comme épisodes complets. Fix recommandé: filtre sample dans
   reclean + extraction RAR (ou quarantaine des releases RAR-only en 098-AUTRES).
2. **DEV #2 (à investiguer) — Match TVDB faible sur titre traduit.** "Murder Mindfully" (titre EN) vs primaire TVDB
   "Achtsam Morden" (DE) → conf 0.38 < seuil → non scrapé. Le scraper ne consulte pas les alias/traductions TVDB.
   Fix recommandé: matcher contre les alias TVDB (translations endpoint) ou abaisser le seuil avec garde année.

**État BDD pré-existant (hors scope de ce run, mais bloquant pour la maintenance):**
3. **DEV #3 (majeur) — 11 FK orphans dans library.db** bloquent `library-reconcile`. Empêche le drain du backlog
   de maintenance (AN/AO/AP/AQ, 88 repairs pending). Cleanup opérateur des 11 lignes orphelines = déblocage.

**Régression précédente résolue:** le split Rick & Morty (run 11h08) est corrigé et n'a pas récidivé.

**Faux positifs d'agents rejetés (evidence-based):** 25 TV canonical=tmdb (DESIGN_CONFORM TVDB-fallback),
65 NULL canonical (entrées author/ebook jamais scrapées, backlog connu), AT qBit (transient).

**Aucun dispatch lancé. Aucun média modifié. Mode read-only — aucun commit.**

## Traitement

(read-only mode — PHASE 4 no-op. Pour corriger DEV #1/#2/#3: lancer `/implement:feature`.)
