# Phase 13 — PR Review Fixes (cycle 2)

> Generated by `/implement:pr-review` cycle 2 (2026-06-03). Both findings were
> confirmed by hand. One sub-phase = one commit, each with a regression test.

## Gate

- CI green at `47e46635`; `make check` green.

## Steps

### 13.1 — qBit `add()` 401 catch (MEDIUM) + Transmission dup robustness (MINOR)

**Files:** `personalscraper/api/torrent/qbittorrent.py`, `personalscraper/api/torrent/transmission.py`, `tests/unit/test_qbittorrent_add.py`, `tests/unit/test_transmission_add.py`

- **MEDIUM (qBit 401)**: `add()` maps "401 → ApiError" via `except qbittorrentapi.LoginFailed`, but a real 401 on `torrents_add` is `qbittorrentapi.Unauthorized401Error` — a DISTINCT class (`HTTP401Error` MRO, NOT a `LoginFailed` subclass). Confirmed: a simulated `Unauthorized401Error` escapes `add()` uncaught. Fix: catch `(qbittorrentapi.LoginFailed, qbittorrentapi.Unauthorized401Error)` → `ApiError(http_status=401)` (keep `LoginFailed` defensively). Verify the docstring's "401 → ApiError" is now accurate.
  - Regression test: `torrents_add` `side_effect=qbittorrentapi.Unauthorized401Error(...)` → `add()` raises `ApiError` with `http_status == 401` (FAILS at baseline — escapes today).
- **MINOR (Transmission dup)**: broaden the except-branch duplicate match to BOTH the lib's result-key form and the daemon-error form: `("torrent-duplicate" in msg or "duplicate torrent" in msg)` — while still re-raising `"duplicate label rejected"` (matches neither). Add a realistic test: a duplicate add that RETURNS a `Torrent` (the installed-lib happy path, no raise) → idempotent success; keep the non-duplicate re-raise test. Optionally add a `"duplicate torrent"` raised-error test → idempotent.

Commit: `fix(torrent-write): qBit catches Unauthorized401Error (401→ApiError) + Transmission dup match robustness`

## Phase gate

- [ ] `make check` green; `audit_design_coverage --strict` 0 findings; smoke.
- [ ] 401 + dup regression tests fail at pre-fix SHA, pass after.
- [ ] Gate commit: `chore(torrent-write): phase 13 gate — PR review fixes cycle 2`.
