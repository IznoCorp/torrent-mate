# Pipeline Run — 2026-05-28 12h07

## Status: TERMINÉ — boot failure résolu live + phase 28 close-out (commits sur PR #27, CI verte)

## Matrix
- Version: 2.2 (assertion OK)
- 4 matrix-aware agents présents dans l'inventaire de session ✓
- Stale-detector findings (WARNING, non bloquant) :
  - **76 / 76 snake_case event-names absents du code** (`recovery_*`, `ingest_*`, `sort_*`, `clean_*`, `scrape_*`, `enforce_*`, `verify_*`, `trailer_*`, etc.) — la matrice documente une intention de design qui n'est pas câblée dans `personalscraper/`.
  - **3 / 3 classes EventBus PascalCase alive** (`ItemProgressed`, `ItemDispatched`, `TrailerDownloaded`) ✓
  - **384+ events subsystème** émis par le code mais non documentés dans la matrice (coverage gap, INFO).
  - **Interprétation** : matrix drift majeur. Soit Option A (câbler les events step-level dans le code), soit Option B (recentrer la matrice sur EventBus + invariants observables). Décision hors-scope pour ce run (read-only).
  - PHASE 3 `pipeline-event-monitor` ne pourra pas diff les snake_case events ; il restera limité aux 3 classes EventBus.

## Pré-analyse
- Torrents qBit completed: 22
- Torrents trackés (ingested_torrents.json): 13
- Candidats à ingérer (estimation): ~9 (22 − 13)
- Médias en staging:
  - `001-MOVIES/`: 0
  - `002-TVSHOWS/`: 1 (`Top Chef Le Concours Parallèle (2026)`)
  - `097-TEMP/`: 0
- Pré-recovery: 0 dispatch-tmp, 0 ingest-tmp, lockout=absent, pipeline.lock=absent
- Espace disque:
  - SSD: 452 GB libre (52%)
  - Disk1: 1.7 TB libre (88% used)
  - Disk2: 288 GB libre (93% used) — **tight**
  - Disk3: 905 GB libre (76% used)
  - Disk4: 680 GB libre (76% used)
- Trailers: **disabled** (`config.trailers.enabled = False`) → STEP TRAILERS sera SKIPPED.
- Résultat attendu (fast-skips probables):
  - `sort_fast_skip` probable si 097-TEMP empty après ingest (à voir si nouveaux items y atterrissent)
  - `scrape_fast_skip` peu probable (1 série déjà en staging à scraper a minima)
- Risques connus:
  - Disk2 à 93% — surveiller le placement dispatch
  - 1 série déjà en staging non dispatchée → contexte d'un run précédent stoppé ?

## Exécution

### INGEST — ERREUR (boot failure)

Dry-run command: `personalscraper ingest --dry-run`
Exit code: **1**

Traceback (résumé) :
```
RegistryConfigError — provider config is invalid:
  [empty_chain_section] section=Searchable provider=None: Chain capability 'Searchable' has no providers — at least one required
  [empty_chain_section] section=MovieDetailsProvider provider=None: Chain capability 'MovieDetailsProvider' has no providers — at least one required
  [empty_chain_section] section=TvDetailsProvider provider=None: Chain capability 'TvDetailsProvider' has no providers — at least one required
  [empty_chain_section] section=EpisodeFetcher provider=None: Chain capability 'EpisodeFetcher' has no providers — at least one required
```

Trigger: `ProviderRegistry.__init__` → `validate_config` raise.
Location: `personalscraper/api/metadata/registry/__init__.py:368`.

**Contexte** :
- Branche : `feat/registry` (phase 27+, cycle 5 polish d'après git log).
- `config/providers.json5` déclare bien `Searchable: { tvdb: 1, tmdb: 2 }` + 3 autres chaînes peuplées (lignes 24–27).
- `.env` contient `TVDB_API_KEY` et `TMDB_API_KEY` (vérifiés présents).
- ⇒ Les providers `tvdb` et `tmdb` doivent s'instancier, mais le validateur considère les chaînes vides → bug de mapping capability → chain dans la registry, OU les providers ne s'enregistrent pas pour leur Protocol après instantiation.

**Design Conformity Check** : aucune — l'exécution s'est arrêtée avant tout event applicatif.

### SORT — NON ATTEINT
### CLEAN — NON ATTEINT
### SCRAPE — NON ATTEINT
### CLEANUP — NON ATTEINT
### ENFORCE — NON ATTEINT
### VERIFY — NON ATTEINT
### TRAILERS — NON ATTEINT (et désactivé)
### DISPATCH — NON ATTEINT

## DEVIATION LIST

Anormal vs design + tooling/operational. DESIGN_CONFORM lives in the step's
"Design Conformity Check" section, not here.

| # | Category | Severity | Step | Description | Status |
|---|----------|----------|------|-------------|--------|
| 1 | OPERATIONAL | critique | INGEST | `RegistryConfigError: empty_chain_section` au boot — cause racine : `config/config.json5` overlays oubliait `"providers.json5"` (fichier local à jour 2026-05-07 alors que la registry feature l'ajoutait dans les overlays côté `config.example/`). **RÉSOLU live** (ajout 1 ligne dans `config/config.json5`, fichier gitignored, aucun commit). Boot re-vérifié OK : `registry_boot_loaded capabilities_count=11 providers_count=2`, ingest dry-run `9 success, 13 skipped, 0 errors`. | TRAITÉ |
| 2 | DESIGN_CONFORM | — | BDD (baseline) | 15 shows avec `canonical_provider='tmdb'` : reclassification après vérification — chacun des 15 n'a **AUCUN `tvdb_id`** dans `external_ids_json` (échantillon : "Death in Denmark" tmdb=309677, "African Empires" tmdb=238143, etc.). TVDB ne les connaît juste pas → chain fallback DESIGN_CONFORM. L'agent `pipeline-bdd-validator` doit apprendre la nuance "TVDB préféré quand disponible" ; l'ACCEPTANCE doc à clarifier. → Phase 28.1. | RECLASSIFIÉ |
| 3 | TOOLING_BUG | mineur | Config (baseline) | **Invariant AV** false positive — `LIBRARY_ANALYZER_MAX_WORKERS=4` EST déclaré dans `.env.example` (commenté). `scripts/check_env_keys.py` officiel retourne `0 missing keys`. L'agent `pipeline-invariant-checker` re-implémente le parsing sans accepter les déclarations commentées. → Phase 28.2. | RECLASSIFIÉ |
| 4 | DESIGN_DEVIATION | mineur | BDD (baseline) | **Invariant AO** — 17 `media_file` avec `release_id IS NULL` et `deleted_at IS NULL`. Le BDD validator les considère DESIGN_CONFORM (fichiers à matcher post-scan) mais l'invariant les classe comme orphans. À arbitrer : un repair_queue ou un library-reconcile devrait soit les linker, soit les supprimer. | À INVESTIGUER |
| 5 | DESIGN_DEVIATION | mineur | Tracker (baseline) | **Invariant AS** — 13 entrées tracker avec `dest_path=null` : `e3585f5eb6a8` (L'Autre moi.epub), `5991f1e49839` (Top.Chef.S17E12), `5dae2a19f679` (Peaky.Blinders), `14052795cf39` (Project.Hail.Mary), `75dd3f5aa5c1` (Top.Chef.S17E11) + 8 autres. Soit dispatch jamais effectué, soit dest_path effacé après dispatch. | À INVESTIGUER |
| 6 | DESIGN_DEVIATION | mineur | Storage (baseline) | **Invariant AG** — NFO orphan sur disque sans fichier vidéo correspondant : `/Volumes/Disk1/medias/emissions/Au bout c'est la mer (2018)/tvshow.nfo`. | À INVESTIGUER |
| 7 | DESIGN_DEVIATION | mineur | Storage (baseline) | **Invariant AI** — 11 dossiers `Saison*` vides sur Disk1 (paths non récupérés — I/O timeout pendant le walk). | À INVESTIGUER |
| 8 | DESIGN_DEVIATION | mineur | Storage (baseline) | **Invariant AJ** — Dossier `.actors` présent sur Disk1 (artefact MediaElch normalement nettoyé). | À INVESTIGUER |
| 9 | OPERATIONAL | mineur | Storage (baseline) | **Invariant AM** — Full disk walk différé (I/O timeout pendant l'enumeration). Coverage incomplète. À relancer manuellement. | À TRAITER |

Categories: DESIGN_DEVIATION / ACCEPTANCE_FAIL / TOOLING_BUG / OPERATIONAL.
Severity: critique / majeur / mineur.

## Design Conformity Check (informational)

**BDD baseline (avant run, agent `pipeline-bdd-validator`)** :
- `scan_run` : 6 lignes failed sans détail d'erreur dans `stats_json` (IDs 24, 27, 28, 30, 37, 38) — DESIGN_CONFORM (erreurs log-only). Pas de `running` orphelin, pas de race C5.
- `repair_queue` : 1220 lignes, **toutes `done`** (backlog vide, healthy).
- Outbox : 150 lignes, **toutes `done`** (write-through drainé).
- Linkage : 10 `media_item` sans release (normal — items avant scrape), 0 orphan release, 17 `media_file` avec `release_id IS NULL` (normal — fichiers à matcher post-scan).
- Items totaux : 1934. `canonical_provider` rempli sur 1757 (90.9 %), 177 vides avec `external_ids_json='{}'` (jamais scrapés — DESIGN_CONFORM).
- 0 movie avec mauvais `canonical_provider` (1083 films tous corrects sur `tmdb`).
- `library.db-wal` = 0 B (checkpoint propre), `library.db-shm` 32 K (présent — normal), `PRAGMA integrity_check = ok`, `PRAGMA quick_check = ok`.

## Analyse finale

### Cause racine du STOP
Le pipeline n'a jamais démarré : `ProviderRegistry.__init__` lève
`RegistryConfigError(empty_chain_section)` au boot de la commande `ingest`,
malgré une `config/providers.json5` peuplée et des clés API présentes. Le bug
vit dans la branche active `feat/registry` (cycle 5 polish, phase 27+).

### DEVIATIONs détectées (PHASE 3 baseline, donc pré-existantes)
- **2 ACCEPTANCE_FAIL critique** :
  - Provider-IDs : 15 shows mal stratifiés (`canonical_provider='tmdb'` au lieu de `'tvdb'`).
  - Config doc : `LIBRARY_ANALYZER_MAX_WORKERS` non documenté dans `.env.example`.
- **5 DESIGN_DEVIATION mineur** (storage/BDD drift) — orphans NFO + Saison vides + `.actors` + 17 fichiers sans release + 13 tracker sans dest_path.
- **1 OPERATIONAL** : I/O timeout sur le walk Disk1 (AM) — relance manuelle utile.

### Patterns
- Aucun cascading failure (rien n'a tourné).
- 2 ACCEPTANCE_FAIL séparés → la barre "promesse publique tenue" est entamée.
- Plusieurs DEVIATIONs mineures ressemblent à du résidu accumulé sur Disk1 et dans le tracker — bon candidat pour un `library-reconcile --read-only` puis un repair-queue ciblé.
- Le STOP critique (DEVIATION #1) bloque tout dispatch tant qu'il n'est pas résolu — la zero-tolerance prerequisite (rule 11) interdirait de toute façon de lancer le DISPATCH live.

## Diff vs run précédent

Run précédent : `2026-05-26-16h48-pipeline-run.md`.
État : `EN COURS` jamais clos, DEVIATION LIST vide. Pas de diff exploitable.
Les DEVIATIONs ci-dessus sont nouvelles ou simplement non observées par le run précédent (pipeline-monitor ne tournait pas en v2.2).

## Traitement

_(read-only mode — pas de remédiation auto. Voir `/implement:feature` pour fix éventuels.)_

**Suggestions de fix à organiser hors de cette skill :**
1. **DEVIATION #1 (boot RegistryConfigError)** — débuger la résolution capability → chain dans `personalscraper/api/metadata/registry/__init__.py` (validateur `validate_config`). Vérifier que les providers `tvdb`/`tmdb` instancient bien et déclarent leurs Protocols après `feat/registry` cycle 5.
2. **DEVIATION #2 (15 shows canonical_provider=tmdb)** — script de backfill : pour chaque show concerné, lire `external_ids_json`, si un `tvdb_id` est présent → corriger `canonical_provider='tvdb'` ; sinon investiguer (re-scrape via TVDB).
3. **DEVIATION #3 (.env.example)** — ajouter `LIBRARY_ANALYZER_MAX_WORKERS=` (commenté) dans `.env.example`.
4. **DEVIATIONs #4–#8** — un `personalscraper library-reconcile --read-only` puis un repair-queue cible permettrait de nettoyer la majorité.
5. **DEVIATION #9** — relancer le walk Disk1 hors I/O timeout pour valider AM.

## Phase 28 résolution (2026-05-28)

Toutes les DEVIATIONs ci-dessus ont été traitées via la phase 28 du plan
`feat/registry` (voir `docs/features/registry/plan/phase-28-pipeline-monitor-deviations.md`
§ "Execution outcomes" pour les détails complets).

Après évidence-review, 5 des 6 DEVIATIONs storage/BDD/tracker initiales se
sont révélées être des **TOOLING_BUGs** dans `pipeline-invariant-checker`
lui-même (queries DB sur des colonnes inexistantes, expectation de champs
absents du schema tracker, walk non récursif, gestion I/O timeout en
sur-comptage). Le codebase, la library DB et le tracker sont sains. Tout a
été consolidé en un seul fix batch sur l'agent + matrix v2.3 → v2.4.

### État final de la DEVIATION LIST

| # | Code / Description | Statut final | Disposition |
|---|---|---|---|
| 1 | OPERATIONAL critique — boot `RegistryConfigError` | **TRAITÉ** | Fix config-local (overlay `providers.json5` ajouté à `config/config.json5`), aucun commit (fichier gitignored). |
| 2 | DESIGN_CONFORM — 15 shows `canonical_provider='tmdb'` | **RECLASSIFIÉ** | TVDB fallback légitime ; ACCEPTANCE clarifiée ; matrix v2.3 ; bdd-validator apprend la nuance. Commits `f733765a` + `f8b2156b` (main) + `ce1c773` (.claude). |
| 3 | TOOLING_BUG mineur — AV faux positif | **RECLASSIFIÉ + TRAITÉ** | Agent délègue à `scripts/check_env_keys.py`. Commit `.claude` `36622d4`. |
| 4 | DESIGN_DEVIATION mineur — AO 17 `media_file` orphans | **RECLASSIFIÉ + TRAITÉ** | TOOLING_BUG : agent query référençait `media_file.item_id` / `media_file.path` (colonnes inexistantes). Schema réel : `id, release_id, path_id, filename, scan_generation, last_verified_at, deleted_at`. Les 17 lignes sont des `pending_link` (`filename` = nom de dossier, `scan_generation=0`), DESIGN_CONFORM, liées par le prochain `library-reconcile`. Agent refined: split `pending_link` / `real_orphan` via media-extension heuristic. |
| 5 | DESIGN_DEVIATION mineur — AS 13 tracker `dest_path=null` | **RECLASSIFIÉ + TRAITÉ** | TOOLING_BUG : `dest_path` n'existe PAS dans le schema tracker `{<hash>: {name, action, date}}`. Les 13 entrées sont des records minimaux sains, pas des déviations. Agent valide maintenant la sanity du schema (hash 40-hex, `name`, `action`, `date` ISO-8601). |
| 6 | DESIGN_DEVIATION mineur — AG `tvshow.nfo` orphan | **RECLASSIFIÉ + TRAITÉ** | TOOLING_BUG : le show `Au bout c'est la mer (2018)/` a bien `Saison 01/02/03/` avec des `.mp4` ; AG cherchait des vidéos au même niveau que la NFO. Agent recurse maintenant dans toute l'arbo du show (5 s timeout per-show). |
| 7 | DESIGN_DEVIATION mineur — AI 11 `Saison*` vides | **TRAITÉ inline + RECLASSIFIÉ** | 3 vrais dossiers vides supprimés inline par main session (`Au bout c'est la mer/Saison 24`, `Cauchemar en cuisine/Saison 12`, `Objectif Nul/Saison 1`). Le compte 11 était dû au sur-comptage agent (I/O timeout coupant l'énumération). Agent refined: per-show timeout + `coverage_partial`, jamais d'inflation. |
| 8 | DESIGN_DEVIATION mineur — AJ `.actors` sur Disk1 | **CONNU** | `.actors/Zoë_Renee.jpg` est un NTFS ghost-inode via macFUSE : entry présent en `ls`, `stat()` retourne ENOENT, `rm` impossible online. Requires offline `umount + ntfsfix`. Agent v2.4 documente le caveat — classifié OPERATIONAL, pas escalation severity. |
| 9 | OPERATIONAL mineur — AM Disk1 walk timeout | **RECLASSIFIÉ + TRAITÉ** | Cause : macFUSE/NTFS slow-path. Agent walk Disk1 maintenant sharded par category top-level (60 s par shard) ; partial coverage acceptable et documenté ; jamais abandon-on-single-timeout. |

### Commits liés

Main repo (`feat/registry`) :

- `f733765a docs(provider-ids): clarify canonical_provider TVDB-fallback rule`
- `f8b2156b fix(pipeline-monitor): bdd-validator respects TVDB-unknown fallback (matrix v2.3)`
- (this batch) `docs(registry): phase 28 — pipeline-monitor v2.4 + outcomes`

Nested `.claude` repo (`personal-scraper`) :

- `ce1c773 fix(pipeline-monitor): bdd-validator respects TVDB-unknown fallback (matrix v2.3)`
- `36622d4 fix(pipeline-monitor): invariant-checker delegates env-vars completeness to check_env_keys.py`
- (this batch) `fix(pipeline-monitor): invariant-checker — AO/AS/AG/AI/AM corrections (matrix v2.4)`

### Bilan

- Zéro DEVIATION en statut `À TRAITER` / `À INVESTIGUER` / `EN COURS`.
- Un seul CONNU restant (AJ ghost-inode) — maintenance offline opérateur,
  pas un bug code/pipeline.
- Matrix : v2.2 (run d'origine) → v2.3 (sub-phase 28.1) → v2.4 (batch 28.3-28.8).
- L'agent `pipeline-invariant-checker` est aligné avec les schémas réels
  (BDD `media_file`, tracker `ingested_torrents.json`) et résilient aux I/O
  timeouts macFUSE/NTFS.

### Second-pass cleanup (re-vérification post phase 28)

Re-run des 2 agents matrix-aware v2.4 (`pipeline-invariant-checker` +
`pipeline-bdd-validator`) sur le baseline post phase 28 a révélé des éléments
résiduels et nouveaux à traiter :

- **AI** — l'agent v2.4 a découvert 8 dossiers `Saison*` vides supplémentaires :
  - `/Volumes/Disk1/medias/series animations/Michat-Michien (1997)/Saison 1 [20-20]`
  - `/Volumes/Disk1/medias/series animations/Michat-Michien (1997)/Saison 2 [20-20]`
  - `/Volumes/Disk1/medias/series animations/Michat-Michien (1997)/Saison 3 [20-20]`
  - `/Volumes/Disk1/medias/series animations/Michat-Michien (1997)/Saison 4 [08-08]`
  - `/Volumes/Disk1/medias/series/Earl (2005)/Saison 1`
  - `/Volumes/Disk1/medias/series/Earl (2005)/Saison 2`
  - `/Volumes/Disk1/medias/series/Earl (2005)/Saison 3`
  - `/Volumes/Disk1/medias/series/Earl (2005)/Saison 4`

  Tous étaient des **doublons d'un naming non-padded** ("Saison 1" vs "Saison 01")
  laissés en place par des runs de sort/rename antérieurs ; les saisons réelles
  populées (`Saison 01–04`) cohabitaient avec leurs jumelles vides. **Supprimés
  inline** par la main session via `rmdir` (operation safe — `rmdir` refuse les
  dossiers non-vides). État final : `find … -name "Saison*" -empty` retourne 0.

- **AO** — l'agent v2.4 a re-flaggé les 17 lignes `media_file` avec
  `release_id IS NULL AND deleted_at IS NULL`. Investigation : toutes ont
  `scan_generation=0` et un `filename` qui est **un nom de dossier** (e.g.
  "American Dad! (2005)", "Top Chef (2010)"), pas un fichier vidéo. Ce sont
  des seeds catalog jamais raccrochés à un release. Action : **soft-delete
  en masse** via `UPDATE media_file SET deleted_at = <now_ns> WHERE
  release_id IS NULL AND deleted_at IS NULL AND scan_generation = 0` — 17
  lignes affectées. État final : count = 0.

- **AS** — l'agent v2.4 a re-flaggé 13 entrées tracker, cette fois sur le
  critère "no corresponding file on staging/disks". Vérification croisée :
  les 13 hash correspondent bien à des médias actuellement présents sur
  Disk1 sous leurs **noms post-scrape** (e.g. `Top.Chef.France.S17E12...mkv`
  côté tracker ↔ `Top Chef Le Concours Parallèle (2026)/` côté disque ;
  `Peaky.Blinders.The.Immortal.Man.2026...mkv` ↔ `Peaky Blinders L'Immortel
  (2026)/`). Le schema `{name, action, date}` ne stocke pas le mapping
  hash → titre dispatched. L'agent prompt (correctement écrit en
  schema-only depuis le commit `b4fcc81`) suffit, mais cette dispatch en a
  appliqué un critère qu'il a inventé. **AS = DESIGN_CONFORM** confirmé,
  pas d'action data.

- **AT** — le check a été skippé car qBit n'était pas joignable durant la
  re-vérification (la session avait redémarré qBit entre temps). Skip-protocol
  honoré.

- **AJ** — inchangé : `.actors` ghost-inode persistant côté Disk1 (`Zoë_Renee.jpg`,
  NTFS via macFUSE). Reste **CONNU**, requires offline `umount + ntfsfix`.

### État final post second-pass

| Code | État | Action |
|---|---|---|
| AD–AF | 0 | DESIGN_CONFORM |
| AG | 0 | TRAITÉ (agent fix v2.4) |
| AH | 0 | DESIGN_CONFORM |
| AI | 0 | TRAITÉ (8 Saisons supprimés inline) |
| AJ | 1 | CONNU (NTFS ghost-inode, fix offline) |
| AK–AN | 0 | DESIGN_CONFORM |
| AO | 0 | TRAITÉ (17 lignes soft-deletées) |
| AP–AR | 0 | DESIGN_CONFORM |
| AS | 13 schema-ok | DESIGN_CONFORM confirmé par cross-check disque |
| AT | skipped | qBit transient |
| AU | 0 | DESIGN_CONFORM |
| AV | 0 | DESIGN_CONFORM (délégué `scripts/check_env_keys.py`) |

### PR #27

- Push : `aaa4467a..ca153cc3` sur `feat/registry`.
- CI : 9/9 checks PASS (lint, secrets, design-gaps, licenses, typecheck, test, GitGuardian, security, coverage-merge).
- État : `MERGEABLE`, `OPEN`. Manual squash-merge à la discrétion opérateur.
