# Pipeline Run — 2026-05-25 09h57

## Status: STOPPÉ — Dispatch bloqué par zero-tolerance (rule 11), opérateur a choisi de fixer les bugs via /implement:feature avant de re-lancer.

## Matrix
- Version: 2.1
- Skill version: 2.1
- Stale-detector findings: 12 stale entries detected (informational, non-blocking)
  - Pre-run recovery: matrix declares `recovery_*` events; code emits `crash_recovery_*` (4 events)
  - PROCESS:scrape: 4 TMDB auto-recovery events declared, zero code matches (`tmdb_id_auto_recovered`, `tmdb_id_validation_skipped`, `tmdb_id_year_widened_retry`, `tmdb_id_unrecoverable_no_match`)
  - PROCESS:scrape: provider-IDs events `external_ids_written`, `canonical_provider` declared but absent in code
  - TRAILERS: `TrailerDownloaded` EventBus class defined but never emitted
  - Coverage gaps: `crash_recovery_dispatch_orphan`, `crash_recovery_lockout_cleaned`, `crash_recovery_done`, `provider_lock_engaged`, `episode_unmatched_no_rename`, `show_season_empty`, `verify_item_done` emitted by code but missing from matrix
- Branch context: running on `fix/tech-debt` (CLAUDE.md mentions `feat/event-bus` as current feature — flagged for operator awareness)

## Pré-analyse
- Torrents complétés (qBit): 36
- Torrents tracked (`.data/ingested_torrents.json`): 31
- Drift: 5 torrents potentially un-tracked (candidates for INGEST)
- Médias en staging: 3 films (001-MOVIES) + 8 séries (002-TVSHOWS)
- Items dans 097-TEMP: 0 (vide)
- Pré-recovery: 0 `_tmp_dispatch_*` orphans, 0 `_tmp_ingest_*`, no qBit lockout, no pipeline.lock
- Espace disque:
  - SSD: 390 GB libre (59%)
  - Disk1: 1.7 TB libre (87% used)
  - Disk2: 290 GB libre (93% used — TIGHT)
  - Disk3: 927 GB libre (76%)
  - Disk4: 680 GB libre (76%)
- Trailers: `config.trailers.enabled = False` → step will be **SKIPPED**
- Résultat attendu (fast-skips probables):
  - `sort_fast_skip` LIKELY (097-TEMP vide → INGEST devra y déposer d'abord; si INGEST 0-effect → SORT fast-skip)
  - `scrape_fast_skip` POSSIBLE (NFOs des items en staging probablement déjà valides puisque le run précédent est du 2026-05-21)
  - `clean_fast_skip` POSSIBLE (pas de pollution détectée à vue)
  - `cleanup_fast_skip` POSSIBLE
- Risques connus:
  - Disk2 à 93% — si DISPATCH y route un gros item, risque `No space left`
  - Stale-detector signale drift matrix v2.1 sur recovery/TMDB-auto-recovery — observer les outputs de pré-recovery + scrape pour confirmer
  - 5 torrents potentiellement non-tracked — INGEST devrait les copier vers 097-TEMP

Liste des 11 items déjà en staging :
- 001-MOVIES: `I Origins (2014)`, `Peaky Blinders L'Immortel (2026)`, `Projet Dernière Chance (2026)`
- 002-TVSHOWS: `American Dad! (2005)`, `Dexter New Blood (2021)`, `FROM (2022)`, `LOL Qui rit, sort ! (2021)`, `Stranger Things Tales from '85 (2026)`, `The Boys (2019)`, `Top Chef (2010)`, `Top Chef Le Concours Parallèle (2026)`

## Exécution

### INGEST — OK (5 ingérés / 31 skip / 0 erreurs / ~145s)

**Items ingérés (5)** :
1. `Mikado.2025.VFF.1080p.WEBRip.EAC3.5.1.x265-TyHD.mkv` (film) — 7s
2. `Imperfect.Women.S01.MULTi.VFF.1080p.WEB.AC3.5.1.H265-TFA` (série) — 85s (saison complète)
3. `Maximum.Pleasure.Guaranteed.S01E02.MULTi.1080p.WEB.H265-FW` (série) — 18s
4. `Dossier.137.2025.FRENCH.1080p.WEBrip.10.bits.EAC3.5.1.x265-TyHD.mkv` (film) — 13s
5. `Maximum.Pleasure.Guaranteed.S01E01.MULTi.1080p.WEB.H265-FW` (série) — 22s

**Design Conformity Check** :
- `torrents_found completed=36 total=36` → DESIGN_CONFORM
- 5× `transfer_complete action=copy` → DESIGN_CONFORM
- 5× `torrent_marked action=copied` → DESIGN_CONFORM (tracker mis à jour)
- `ingest_complete errors=0 skipped=31 success=5` → DESIGN_CONFORM (idempotence)
- 31 entrées tracker avec `dest_path: null` → DESIGN_CONFORM (legacy pre-dest_path entries — déjà dispatchées sur disques)
- `.DS_Store` files dans staging → DESIGN_CONFORM (cosmétique macOS)

**Agents** : 4/4 CLEAN (orphan-hunter, state-validator, output-analyzer, ingest-checker)
- Tracker post-INGEST = 36 entries (31 legacy + 5 nouvelles avec dest_path sous 097-TEMP)
- 0 orphans, 0 pipeline.lock, 0 missed torrents
- Timing NORMAL, 0 warnings, 0 errors
### SORT — OK (5 déplacés / 0 erreurs / ~35ms)

**Items déplacés** :
- `Dossier.137.2025.*.mkv` → `001-MOVIES/Dossier 137 (2025)/...`
- `Mikado.2025.*.mkv` → `001-MOVIES/Mikado (2025)/...`
- `Imperfect.Women.S01.*` → `002-TVSHOWS/Imperfect Women/...`
- `Maximum.Pleasure.Guaranteed.S01E01.*` → `002-TVSHOWS/Maximum Pleasure Guaranteed/...`
- `Maximum.Pleasure.Guaranteed.S01E02.*` → `002-TVSHOWS/Maximum Pleasure Guaranteed/...` (merge même show)

**Design Conformity Check** :
- 5× `sort_item_moved` → DESIGN_CONFORM
- `tracker_dest_path_pruned removed=5` + `sort_tracker_pruned removed=5` → DESIGN_CONFORM
- `sort_complete errors=0 moved=5 skipped=0` → DESIGN_CONFORM
- TV folder names WITHOUT trailing year (`Imperfect Women`, `Maximum Pleasure Guaranteed`) → DESIGN_CONFORM (SCRAPE ajoute l'année)
- Pas de `Saison NN/` créée à ce stade → DESIGN_CONFORM (responsibilité SCRAPE)
- `.DS_Store` partout → DESIGN_CONFORM (cosmétique macOS)
- 097-TEMP vide post-SORT → DESIGN_CONFORM

**Agents** : 4/4 CLEAN (orphan-hunter / state-validator / sort-checker), 1 mineur observation (output-analyzer)
- Pas de displacement des items pré-existants (001-MOVIES 5 items, 002-TVSHOWS 10 items)
- 0 duplicats, 0 orphans, invariants AD/AE/AF/AI/AJ tous OK
- Tracker 36 entries toutes avec `dest_path: null` post-prune

### CLEAN — OK (recleaned=0 skipped=0 errors=0)
- Aucune pollution détectée → DESIGN_CONFORM

### SCRAPE — OK (5 scrapés / 10 skip / 1 unmatched / 0 erreurs / ~6s)

**Films** (1 scrapé / 4 skip / 1 unmatched):
- **Dossier 137 (2025)** → TMDB match confidence 1.0 → mkv renamed (`Dossier 137.mkv`), NFO written, poster.jpg + landscape.jpg downloaded ✅
- I Origins (2014), Peaky Blinders L'Immortel (2026), Projet Dernière Chance (2026) → `nfo_valid skipped_already_done`
- **Mikado (2025)** → `movie_no_tmdb_results` + WARNING `movie_no_confident_match score=0.0` → DESIGN_CONFORM (pas de fallback pour les films, action manuelle MediaElch nécessaire)

**Séries** (4 scrapées / 6 skip / 0 unmatched):
- **FROM (2022)** → `show_rescrape_drift` (canonical_uniqueid manquant sur S04E04) → rescrape, TVDB match, `provider_lock_engaged tvdb suppressed tmdb`, 3 seasons (S02/S03/S04) déjà avec dirs et 20 épisodes already_named, artwork existant
- **Imperfect Women (2026)** → TVDB match (id 448179), folder renommé → `Imperfect Women (2026)`, `provider_lock_engaged tvdb`, `Saison 01` créée, 8 épisodes renommés (S01E01..E08), release dir supprimé, 3 artworks téléchargés (poster/landscape/season01-poster)
- **Maximum Pleasure Guaranteed (2026)** → TVDB match (id 460793), folder renommé → `Maximum Pleasure Guaranteed (2026)`, `provider_lock_engaged tvdb`, `Saison 01` créée, 2 épisodes renommés (S01E01-Magnets, S01E02-YABA), 2 sibling NFOs supprimés, 2 release dirs supprimés, 3 artworks téléchargés
- **Top Chef Le Concours Parallèle (2026)** → `show_rescrape_drift` (S17E10 raw release name) → rescrape, TVDB match, mais **AUCUN `show_season_fetched`, AUCUN `episode_renamed`** — la dérive a été détectée mais la cure n'a pas été appliquée → **VOIR DEVIATION #2**

**Design Conformity Check** :
- `provider_lock_engaged` × 3 (FROM, Imperfect Women, Maximum Pleasure Guaranteed) avec `suppressed_provider=tmdb` → DESIGN_CONFORM (matrix v2.1 + provider-ids ACCEPTANCE #6)
- NFOs valides : Dossier 137 (TMDB 1294698 + IMDB tt34794183), FROM (TVDB 401003 + TMDB 124364 + IMDB tt9813792), Imperfect Women (TVDB 448179 + TMDB 108181 + IMDB tt31937954), Maximum Pleasure Guaranteed (TVDB 460793 + TMDB 285404 + IMDB tt35946742) → DESIGN_CONFORM (provider-ids ACCEPTANCE #4 — canonical_provider=tvdb pour séries, pas de cross-contamination)
- `episode_sibling_deleted` × 2 (.nfo dans release dirs Maximum Pleasure) → DESIGN_CONFORM
- `release_dir_removed` × 3 → DESIGN_CONFORM
- Mikado sans NFO → DESIGN_CONFORM (matrix v2.1 §PROCESS:scrape — pas de fallback movies)
- Top Chef NFO `<title>` = "Top Chef: Le Concours Parallèle" vs folder sans colon → DESIGN_CONFORM (sanitization)
- Top Chef IMDB `<uniqueid>` vide → DESIGN_CONFORM (IMDB optionnel pour TV)

### CLEANUP — OK (removed=0)
- Aucun junk file à supprimer → DESIGN_CONFORM

**Agents** : 3/4 CLEAN (orphan-hunter / state-validator / output-analyzer), 1 deviation détectée par scrape-checker
### ENFORCE — OK (0 fixed / 15 OK / 0 erreurs)

**Output** : `Enforce: 0 fixed, 15 OK, 0 errors`

**Design Conformity Check** :
- Aucune sanitization nécessaire sur les 15 items en staging → DESIGN_CONFORM
- Invariants AD/AE/AF/AI/AJ tous passés
- Pas de regression vs état post-PROCESS

**Agents** : 2 CLEAN (orphan-hunter / state-validator), 1 a flagué 2 TOOLING_BUG → DEVIATIONS #3 et #4 (pré-classifiés par le matrix v2.1 line 281)
### VERIFY — OK (13 OK / 2 blocked / 0 erreurs / ~1ms)

**Blocked items** :
- **Mikado (2025)** → 5/8 checks, errors=['NFO not found: Mikado.nfo', 'Poster not found: Mikado-poster.jpg'] → DESIGN_CONFORM (Unmatched Movie Policy safety net — conséquence de `movie_no_confident_match` en SCRAPE)
- **Top Chef Le Concours Parallèle (2026)** → 15/18 checks, errors=['No Saison XX/ with properly named episodes', 'Unrenamed episodes: ...S17E10...'] → **conséquence downstream de DEVIATION #2** (pas une nouvelle entrée, cité ici comme symptôme)

**Design Conformity Check** :
- 13× `verify_item_done status=valid` → DESIGN_CONFORM
- VERIFY est read-only (matrix §VERIFY contract) → DESIGN_CONFORM
- Mikado blocked → DESIGN_CONFORM (Unmatched Movie Policy)

**Agents** : 1 CLEAN (orphan-hunter), 1 issued findings (state-validator → DEVIATION #5), 1 issued findings (output-analyzer → DEVIATION #6 mineur)
### TRAILERS — SKIPPED (config.trailers.enabled = False)
GATE 5b = SKIPPED (configuration explicite)
### DISPATCH — STOPPED (zero-tolerance gate, real dispatch NOT launched)

**Dry-run plan** (13 OK / 0 skipped / 0 errors — Mikado et Top Chef LCP filtrés par VERIFY interne) :

| # | Action | Item | Target disk |
|---|--------|------|-------------|
| 1 | moved | Dossier 137 (2025) | disk_1 (new) |
| 2 | moved | I Origins (2014) | disk_1 (new) |
| 3 | **replaced** | Peaky Blinders L'Immortel (2026) | disk_1 |
| 4 | moved | Projet Dernière Chance (2026) | disk_1 (new) |
| 5 | merged | American Dad! (2005) | disk_2 |
| 6 | moved | Dexter New Blood (2021) | disk_1 (new) |
| 7 | merged | FROM (2022) | disk_3 |
| 8 | moved | Imperfect Women (2026) | disk_1 (new) |
| 9 | merged | LOL Qui rit, sort ! (2021) | disk_1 |
| 10 | moved | Maximum Pleasure Guaranteed (2026) | disk_1 (new) |
| 11 | moved | Stranger Things Tales from '85 (2026) | disk_1 (new) |
| 12 | merged | The Boys (2019) | disk_2 |
| 13 | merged | Top Chef (2010) | disk_1 |

**Case-insensitive matches** (DESIGN_CONFORM, INFO) :
- staging `FROM (2022)` → disk_3:series/`From (2022)` (disk casing wins sur merge)

**Design Conformity Check** :
- VERIFY ré-exécuté par DISPATCH en interne (15 `verify_item_done` events visibles dans dispatch output) → DESIGN_CONFORM ✅ — invalide DEVIATION #5
- Plan dispatch cohérent avec VERIFY filter (13 valid, 2 blocked exclus)
- Aucune erreur de planification

**Décision opérateur** : STOPPED — le DEVIATION LIST contient 4 entrées MAJEUR + 2 mineur ouvertes. L'opérateur a choisi de stopper le pipeline et traiter TOUS les bugs via `/implement:feature` avant de relancer.

## DEVIATION LIST

Anormal vs design + tooling/operational. DESIGN_CONFORM lives in the step's
"Design Conformity Check" section, not here.

| # | Category | Severity | Step | Description | Status |
|---|----------|----------|------|-------------|--------|
| 1 | DESIGN_DEVIATION | mineur | SORT | `cli.invoke.sort` / `cli.complete.sort` log lines absent (alors que INGEST émet `cli.invoke.ingest` + `cli.complete.ingest`). Inconsistence dans le bracketing CLI lifecycle entre les sous-commandes. À déterminer si by-design (suppression intentionnelle en pipeline mode) ou bug logging. | À INVESTIGUER |
| 2 | DESIGN_DEVIATION | majeur | SCRAPE | **Top Chef Le Concours Parallèle (2026)** : `show_rescrape_drift` fired with reason `episode_naming_drift:Top.Chef.Le.Concours.Parallele.S17E10.FRENCH.1080p.WEB.H264-laRoulade.mkv`. Show matched on TVDB (id 475278, confidence 1.0), tvshow.nfo réécrit, MAIS aucun `show_season_fetched`, aucun `episode_renamed`. Le fichier reste avec son nom raw release dans `Saison 17/`. **La dérive a été détectée mais la cure d'épisode n'a pas été appliquée.** Bloque GATE 6 (zero-tolerance). Pipeline : la branche rescrape semble shortcircuit après tvshow.nfo write quand artwork est déjà présent, sans atteindre l'episode-rename phase. | À INVESTIGUER |
| 3 | TOOLING_BUG | majeur | ENFORCE | ENFORCE émet **zero structlog INFO events** (uniquement le summary CLI `Enforce: 0 fixed, 15 OK, 0 errors`). Les events `enforce_start`, `enforce_sanitize_filename`, `enforce_structure_ok`, `enforce_coherence_ok`, `enforce_complete` listés dans le matrix v2.1 §ENFORCE sont absents. Pre-classifié par matrix line 281. Rend ENFORCE non-monitorable. | À INVESTIGUER |
| 4 | TOOLING_BUG | majeur | ENFORCE | Aucun `ItemProgressed(step="enforce", ...)` émis sur l'event-bus. Pre-classifié par matrix line 281 : "Wiring event-bus manquant côté enforce (peut-être un oubli du scope event-bus)". Possible oubli scope event-bus feature. | À INVESTIGUER |
| 5 | DESIGN_DEVIATION | majeur | VERIFY | ~~VERIFY blocked status non persisté~~ → **INVALIDÉ** : DISPATCH ré-exécute VERIFY en interne (les 15 `verify_item_done` events sont émis depuis dispatch). Le filtre verify est appliqué correctement même quand les étapes sont lancées individuellement. La capacité de DISPATCH à invoquer VERIFY en interne est DESIGN_CONFORM. | NON REPRODUCTIBLE |
| 6 | DESIGN_DEVIATION | mineur | VERIFY | `cli.invoke.verify` / `cli.complete.verify` log lines absent (même pattern que DEVIATION #1 sur SORT). Cohérence CLI lifecycle bracketing partielle entre subcommands. | À INVESTIGUER |
| 7 | **ACCEPTANCE_FAIL** | **critique** | BDD | **provider-ids ACCEPTANCE #4 violation**. 209 items `kind='show'` ont `canonical_provider='tmdb'` au lieu de `'tvdb'`. Tous ces items ont POURTANT le bon TVDB id dans `external_ids_json` → corruption de l'état du canonical_provider, pas un défaut de scrape. Impact : tout downstream qui s'appuie sur `canonical_provider` pour router vers la famille TVDB (backfill épisodes, fallback ranking) échoue pour 209 séries. 142 movies ont aussi `canonical_provider IS NULL`. Remédiation : UPDATE SQL massif après audit de quand ces rows ont été insérées (probablement migration pré-v0.15.0 ou drift scraper). | À INVESTIGUER |
| 8 | DESIGN_DEVIATION | mineur | BDD (AO) | 102 `media_file` rows sans `release_id` (cross-ref invariant AO violé). Schema autorise NULL mais l'invariant attend release_id renseigné. Probable artifact d'ingest/dispatch incomplet ou DB recovery. Confirme `library-reconcile.files_without_release=102`. | À INVESTIGUER |
| 9 | DESIGN_DEVIATION | mineur | BDD (AP) | 3 `season.episode_count` ≠ actual count (item IDs 2163, 3655, 3656). Invariant AP violé : S2163#1 (12→13), S3655#5 (0→16), S3656#6 (0→16). Drift DB post-crash ou scrape incomplet. Confirme `library-reconcile.season_count_drift=3`. | À INVESTIGUER |
| 10 | DESIGN_DEVIATION | majeur | Config (AV) | 3 env keys utilisées dans le code via `os.environ.get()` mais absentes de `.env.example` : `OMDB_API_KEY`, `OMDB_DAILY_LIMIT`, `LIBRARY_ANALYZER_MAX_WORKERS`. Risque silent runtime failure sur CI et nouveaux clones. | À INVESTIGUER |
| 11 | TOOLING_BUG | mineur | BDD persistence | DEVIATION #2 (`show_rescrape_drift` reason `episode_naming_drift`) n'est PAS persistée dans `item_issue` rows. La détection de drift est observation-level uniquement (logs structlog). Conséquence : Top Chef LCP id=28 a 9 épisodes indexés en BDD sans aucune trace du drift. Suggère d'ajouter persistence de `episode_naming_drift` dans item_issue pour audit trail. | À INVESTIGUER |
| 12 | TOOLING_BUG | mineur | BDD stale | Mikado entry (id=518) en BDD avec `canonical_provider='tmdb'`, `external_ids_json` populated, `nfo_status='valid'` — scraped 2026-05-24 11:02. Mais le PROCESS de ce run (2026-05-25 10h08) a produit `movie_no_tmdb_results` + `movie_no_confident_match` pour Mikado. Désynchronisation BDD ↔ état réel staging. Soit l'entry BDD vient d'une tentative antérieure soit l'API TMDB a changé d'état. Possible nettoyage BDD nécessaire pour items unmatched. | À INVESTIGUER |

Categories: DESIGN_DEVIATION / ACCEPTANCE_FAIL / TOOLING_BUG / OPERATIONAL.
Severity: critique / majeur / mineur.

## Traitement

Filled by PHASE 4 only when `--remediate` flag was passed. Read-only mode this run.

## Diff vs run précédent (2026-05-21-17h16)

**Items dispatched** :
- 2026-05-21 : 1 item (From.S04E01) ingéré, dispatch dry-run OK mais HARD gate bloqué (rule 11). Aucun dispatch réel.
- 2026-05-25 (ce run) : 5 items ingérés, dry-run OK pour 13 items, HARD gate à nouveau bloqué (rule 11), aucun dispatch réel. **Pattern de pipeline-monitor : la zero-tolerance bloque systématiquement le dispatch quand il y a des bugs persistants — c'est le design**.

**Bugs persistants entre runs** :
- **Top Chef Le Concours Parallèle S17E10** : déjà bloquant le 2026-05-21 (ENFORCE déplaçait mais ne renommait pas). Ce run : SCRAPE déclenche un rescrape_drift mais ne renomme toujours pas. Le bug évolue mais persiste.
- **ENFORCE émet 0 structlog events** : observation déjà notée le 2026-05-21 (DEVIATION #88 dans previous run sur coverage gaps ENFORCE). Toujours présent.

**Bugs résolus depuis le précédent run** :
- ✅ VERIFY n'émettait AUCUN structlog event sur le précédent run (DEV #94). Ce run, VERIFY émet `verify_item_done` events normalement (visible dans dispatch internal verify call). **Bug fixé entre 2026-05-21 et 2026-05-25**.
- ✅ Matrix v2.0 → v2.1 bump (les 4 agents matrix-aware sont maintenant discoverables, vs DEVIATION critique précédente).

**Nouveaux bugs détectés** :
- **DEVIATION #7 critique (ACCEPTANCE_FAIL)** : 209 séries avec `canonical_provider='tmdb'` au lieu de `'tvdb'` — n'avait pas été investigué dans les runs précédents. Pourrait être une régression liée au scope provider-ids ou un état corrompu de longue date.
- **DEVIATION #10 majeur** : 3 env keys manquantes dans .env.example.

**Stable patterns** :
- 0 orphans (AD/AE) — pré-recovery fait son job correctement.
- Disk2 toujours TENDU (93% used / 290G free).
- Trailers SKIPPED (config disable persistante).

## Analyse finale

### Verdict
Le run a **STOPPÉ AVANT DISPATCH RÉEL** sur décision opérateur, suite à 4 deviations majeures + 1 critique ACCEPTANCE_FAIL bloquant le zero-tolerance gate (rule 11). Aucun dispatch n'a été effectué. L'état staging est intact (13 items prêts pour dispatch, 2 blocked).

### Hiérarchie des bugs à traiter (`/implement:feature` chain proposée)

**P0 — ACCEPTANCE_FAIL critique** (à traiter en premier) :
- **#7** : Provider-IDs canonical_provider corruption (209 shows). Audit timestamp d'insertion + UPDATE SQL massif. **Doit avoir un test de régression** (memory rule).

**P1 — Majeurs bloquants** (rule 11 zero-tolerance) :
- **#2** : Top Chef LCP `show_rescrape_drift` détecté mais episode_rename non appliqué. Vérifier la branche rescrape dans `personalscraper/scraper/tv_service.py` qui shortcircuit après tvshow.nfo write quand artwork existe. **Test de régression à ajouter**.
- **#3** : ENFORCE émet 0 structlog INFO events. Bug long-standing (pré-existait au 2026-05-21).
- **#4** : ENFORCE ne génère pas d'`ItemProgressed(step="enforce")` event-bus emit. Wiring event-bus à ajouter — possible oubli du scope event-bus feature actuelle.
- **#10** : Ajouter `OMDB_API_KEY`, `OMDB_DAILY_LIMIT`, `LIBRARY_ANALYZER_MAX_WORKERS` dans `.env.example` (rapide).

**P2 — Mineurs** :
- **#1, #6** : CLI lifecycle inconsistency — `cli.invoke.sort`/`cli.complete.sort` et `cli.invoke.verify`/`cli.complete.verify` absents. Comparer avec INGEST qui les émet pour normaliser.
- **#8** : 102 media_file sans release_id (invariant AO). Investigation BDD requise.
- **#9** : 3 season_count_drift (item IDs 2163, 3655, 3656). Repair_queue à enrichir.
- **#11** : `episode_naming_drift` non persisté dans item_issue → ajouter audit trail.
- **#12** : Mikado entry BDD stale vs état staging — désync potentielle. Possible nettoyage BDD pour items unmatched.

### Bugs déclassés
- **#5** (VERIFY blocked non persisté) : NON REPRODUCTIBLE — DISPATCH ré-exécute VERIFY en interne. Le filtre est effectif.

### Matrix maintenance
PHASE 0 stale-detector a flagué 12 entrées matrix-vs-code drift (recovery_* → crash_recovery_*, TMDB auto-recovery events absents du code, TrailerDownloaded class jamais émise, provider-IDs events absents du code). Ces drifts sont des candidats pour le bump matrix v2.2.

### Coverage gaps observés (futurs candidats matrix v2.2)
- `nfo_corrupt_rescrape_would_delete` (PROCESS:scrape) — vu dans ce run, à ajouter à §PROCESS:scrape DESIGN_CONFORM.
- `episode_sibling_would_delete` / `episode_sibling_deleted` — pareil.
- `release_dir_removed` — pareil.
- `tracker_dest_path_pruned` / `sort_tracker_pruned` — déjà signalés dans run précédent.

### Stat run
- Durée totale : ~22 min (10h08–10h19 hors agents).
- 0 erreur d'exécution.
- 0 STOP_PROTOCOL trigger (rule 4 / 2.5).
- 13/15 items prêts pour dispatch (87% taux de succès staging → dispatch-ready).
- 1 ACCEPTANCE_FAIL critique + 4 deviations majeures + 6 mineures + 2 déclassées.

### Prochaine action recommandée
1. L'opérateur lance `/implement:feature` avec scope = fix des deviations P0 + P1.
2. Une fois les fix mergés, relancer `/pipeline-monitor` pour vérifier que les bugs ont disparu et le dispatch peut passer.
3. Les items en staging (13 prêts + 2 blocked) restent intacts — re-run sera idempotent vs les items déjà scrapés.
